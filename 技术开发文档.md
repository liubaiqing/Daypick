# Calendar 日历应用 · 技术开发文档

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.2（MVP 设计稿，开发进行中） |
| 产品名称 | calendar |
| 目标平台 | Windows 桌面端（Flutter Windows） |
| 文档状态 | 已评审，可直接据此实现 |
| 最后更新 | 2026-08-29 |

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-08-29 | 初始版本：确定 MVP 范围、技术选型、架构、数据模型、解析引擎、UI 规范、版本控制规范与里程碑 |
| v0.2 | 2026-08-29 | 修正文档日期；按文档进入开发阶段（M0 起，里程碑推进中） |
| v0.3 | 2026-08-29 | 开发落实：OCR 方案定为 winrt_ocr_flutter（windows_ocr 停更核实）；记录 /utf-8 构建修复 |
| v0.4 | 2026-08-29 | 开发推进：M0–M3 完成（工程/设计系统/日历/本地与 AI 解析/OCR/设置页），tag v0.1.0–v0.3.0；修订记录随里程碑更新 |
| v0.5 | 2026-08-29 | MVP 完成：M4 交付（ics 导出/JSON 备份恢复/测试补全/Release 打包），tag v1.0.0；后续进入手动 UAT 与二期 backlog |
| v0.6 | 2026-08-29 | UI 调整：窗口按钮改为 Windows 风格右上角（移除交通灯）；AI 服务改为供应商下拉选择（预设+自定义）；tag v1.0.1 |
| v0.7 | 2026-08-29 | 修复灰屏 bug：DSTokensScope 上移至 MaterialApp.builder（弹层此前无法访问主题，Release 空指针）；补充透明 Material 祖先；新增集成回归测试（快速点击日期/双击表单）；tag v1.0.2 |
| v0.8 | 2026-08-29 | 字体规范落地：显式指定 Segoe UI Variable→微软雅黑回退链；字重统一为 400/700（消除合成加粗）；tag v1.0.3 |
| v0.9 | 2026-08-29 | 字体方案定稿：打包 HarmonyOS Sans SC 作为中英文唯一主字体（含许可合规声明）；tag v1.0.4 |
| v0.10 | 2026-08-29 | 解析规则调整：无年份默认当前系统年（取消"已过取明年"偏移）；窗口启用 Win11 DWM 原生圆角；tag v1.0.5 |
| v0.11 | 2026-08-29 | 交互即时性修复：按钮区与双击手势区分离、日期格移除双击新建（单击不再延迟 300ms），新建入口改为今日列表"＋"按钮；tag v1.0.6 |
| v0.12 | 2026-08-29 | 输入体验重构：移除侧边栏"新建"与整页输入页，改为日历主界面底部 AI 对话式圆角输入条（上传/附件/拖拽/图文并存/回车发送/模式分段），解析结果以独立面板呈现；设置页移除解析模式切换；圆角/hover 风格全面统一；tag v1.1.0 |
| v0.13 | 2026-08-29 | 侧边栏支持边缘把手点击收起/展开（200ms 动画，状态持久化）；tag v1.1.1 |
| v0.14 | 2026-08-29 | 侧边栏把手改为悬停浮现式：默认隐藏，鼠标靠近右边界线时淡入、离开淡出；tag v1.1.2 |
| v0.15 | 2026-08-29 | 收起把手优化：改为 overlay 覆盖交界线（去除 40px 占位空白），按钮缩小为 16×32 贴合分界线；tag v1.1.3 |
| v0.16 | 2026-08-29 | 日期选中聚焦动画：首次从今天弹动/原地浮现，后续丝滑移动（380ms，easeOutBack/easeInOutCubic）；tag v1.1.4 |
| v0.17 | 2026-08-29 | 输入条支持 Ctrl+V 粘贴：剪贴板为图片时作为附件（临时文件随删除清理），为文本时正常粘贴；日期格加防溢出保护；tag v1.1.5 |
| v0.18 | 2026-08-29 | OCR 引擎切换为 RapidOCR（ONNX + PP-OCRv5，跨平台一套模型，无需 MSIX 包身份）；新增 OCR 集成测试（合成中文图片离线识别）；tag v1.2.0 |
| v0.19 | 2026-08-29 | AI 解析年份缺省规则：无年份默认当前系统年（prompt 注入当前年 + 客户端兜底强制/宽松解析），与本地模式对齐；tag v1.2.1 |
| v0.20 | 2026-08-29 | "仅提供日"规则（双模式）：只给"日"时年月取系统当前（15号=本月15日），含当月天数校验；tag v1.2.2 |
| v0.21 | 2026-08-29 | OCR 后处理：阅读顺序排序 + 同行/折行合并（恢复被排版换行截断的完整句）；解析层标题行过滤与关键词排除后缀（通知类图片从 4 个垃圾事件 → 1 个正确事件）；tag v1.2.3 |
| v0.22 | 2026-08-29 | 聚焦动画重构：全程单圆（动画期间隐藏静态选中圆）、Apple 风格缓动曲线、跨月点击在新网格播放动画；设置页新增动画总开关（日期聚焦/侧边栏/弹层过渡）；tag v1.2.4 |
| v0.23 | 2026-08-29 | 修复聚焦动画末段回弹晃动：位置曲线改为无 overshoot（easeOutCubic），弹动感转移至缩放弹性；tag v1.2.5 |
| v0.24 | 2026-08-29 | 修复动画终点"瞬间上移"跳变：动画终点对齐静态圆实际中心（格子中心 −3.5px，与 _DayCell 列内容居中一致）；tag v1.2.6 |
| v0.25 | 2026-08-29 | 修复快速连续点击动画消失：whenCompleteOrCancel 在 reset 取消时误隐藏动画圆，改用 whenComplete；快速点击时从当前插值位置续接动画；tag v1.2.7 |
| v0.26 | 2026-08-29 | 界面重构：删除侧边栏与收起把手（日历占满全屏）；设置改为左下角圆形按钮 + 弹窗小窗；输入条底部居中减短（maxWidth 640）；tag v1.3.0 |
| v0.27 | 2026-08-29 | 解析模式分段控件滑块化：本地/AI 切换时选中块白底阴影**平滑滑动**（easeInOutCubicEmphasized，150ms），受设置页动画总开关控制（关闭时瞬间就位）；控件改为**内容自适应**布局（TextPainter 测量段宽），可在 Row 非 flex 槽位使用（Flex 对非 flex 子元素主轴不限制宽度，不得依赖父约束）；tag v1.3.1 |
| v0.28 | 2026-08-29 | 设计补充：**批量删除事件**（设置→数据管理入口，按月列出事件+逐条勾选+全选+已选计数，删除前确认，删除进回收站）与**回收站**（软删除 `deletedAt` 字段、恢复、彻底清理不可恢复警告；单条删除同步改为进回收站；ics/JSON 备份仅含正常事件）；文档先行，代码实现随后，tag 待实现后打 |
| v0.29 | 2026-08-29 | 实现 v0.28 设计：events 表新增 `deletedAt`（schemaVersion 2 + 迁移）；EventDao 软删除/批量软删除/恢复/彻底清理/回收站查询，正常查询与导出统一过滤；新增 DSCheckbox、批量删除窗口（§10.5）、回收站窗口（§10.6）；设置页数据管理入口；单条删除/撤销保存改文案；测试：DAO 6 项、迁移 1 项、Widget 3 项、集成 1 项；tag v1.3.2 |
| v0.30 | 2026-08-29 | 移除回收站入口数量徽标（按钮右侧不再显示待清理数量；删除 trashCountProvider/trashEventsProvider 与对应测试断言）；tag v1.3.3 |
| v0.31 | 2026-08-29 | 月视图顶栏"今天"按钮文案改为"跳回今天"（语义更明确）；同步更新 §10.1 与测试断言；tag v1.3.4 |
| v0.32 | 2026-08-29 | 设置弹窗去重：内容区移除重复的"设置"大标题（标题栏已表达），分区直接承接；tag v1.3.5 |
| v0.33 | 2026-08-29 | 星期表头样式增强：字号 11→12、颜色加深（textSecondary→textPrimary 加粗 w600）、表头下新增分隔线，强化与日期区的层次分界；tag v1.3.6 |
| v0.34 | 2026-08-29 | 月视图年月标题可点击：hover 浮现淡色圆角底色提示；点击弹出 DSYearPicker 年份选择器（12 年一页网格 + 翻页），选中后保持月份跨年跳转；新增对应 widget 测试；tag v1.3.7 |
| v0.35 | 2026-08-29 | 年月标题 hover 提示去除边框线（仅保留淡色圆角底色）；tag v1.3.8 |
| v0.36 | 2026-08-29 | 设计补充：设置主题三选一（浅色/深色/毛玻璃）；深色按 Apple HIG Dark Mode 规范（不纯黑、亮度分层代替阴影、强调色提亮、对比度达标）；毛玻璃按 Apple HIG Materials 规范（blur 20–24 + 饱和度补偿 1.8 + 半透明白着色 + hairline 描边，层次递进）；`themeMode` 设置项与 token 扩展设计；文档先行，代码实现随后，tag 待实现后打 |
| v0.37 | 2026-08-29 | 实现 v0.36 设计：`themeMode` 设置项（默认浅色，不再跟随系统）；`DSTokens` 新增 glass 主题与 dark 弹层色 `#3A3A3C`、投影色 token（深色移除投影）；新增 `DSGlassSurface`（BackdropFilter blur 20 + ColorFilter 饱和度 1.8 + 半透明白 + hairline，浅色/深色退化为普通容器）；设置页外观新增主题三段控件（即时生效并持久化）；弹层/输入条/卡片阴影与背景统一走 token；测试：设置 DAO 2 项、token 1 项、Widget 1 项、集成主题切换；tag v1.3.9 |
| v0.38 | 2026-08-29 | 毛玻璃质感增强：着色改为**反光渐变**（左上白 0.4 → 右下透明 0.1）替代纯色高不透明度；新增**渐变边缘描边**（CustomPaint 1.5px，白 0.5 → 0.12）模拟玻璃边缘反射；阴影改 `spreadRadius: -1` 贴合边缘；输入条接入 DSGlassSurface；主背景玻璃渐变层次；参数依据 devgex/掘金毛玻璃教程；tag v1.4.0 |
| v0.39 | 2026-08-29 | 毛玻璃覆盖范围补全：主界面改"装饰光斑背景 + 全屏 BackdropFilter 模糊层 + 清晰内容层"三层结构（整窗玻璃质感）；设置分区卡片（_SectionCard）接入 DSGlassSurface（嵌套玻璃）；DSGlassSurface 支持非玻璃分支 border/无阴影开关；tag v1.4.1 |
| v0.40 | 2026-08-29 | 修复主界面无透明感：内容层与标题栏背景改为透明（原半透明白盖死模糊层）；模糊层白度降至 0.35→0.18、光斑饱和度提升，光斑经模糊清晰透出；tag v1.4.2 |
| v0.41 | 2026-08-29 | 回归 Apple HIG 标准：**移除装饰光斑与整窗模糊**（用户明确不要假背景）；窗口主背景改实色（极浅白渐变），毛玻璃仅用于浮层（输入条/弹窗/分区卡片模糊背后真实内容）；tag v1.4.3 |
| v0.42 | 2026-08-29 | 修正毛玻璃主题刺眼问题：① **撤销全局强调色改动**（主界面 accentBlue 等保持不变）；② 根因修复——移除 DSGlassSurface 的 ColorFiltered 饱和度补偿（它会作用于容器内所有前景内容，把开关/按钮颜色重新拉饱和）；③ 新增 `softAccentBlue`/`softSuccessGreen` 柔和色 token（浅色/深色与主色一致），仅设置窗口动画开关与保存按钮在毛玻璃主题下使用；tag v1.4.6 |

---

# 1. 项目概述与范围

## 1.1 产品目标

Calendar 是一款 Windows 桌面端"关键事务日历"应用。用户将包含时间、地点、事务内容的**文本**或**图片**交给程序，程序自动抽取其中的关键信息并整理为结构化事件；经用户逐条确认（可编辑）后，事件才写入本地日历。数据独立保存在本机，不做多端同步。

## 1.2 核心用户流程

```
输入（粘贴文本 / 选择或拖入图片 / 粘贴剪贴板图片）
  → （图片时）OCR 识别为文本，回显可编辑
  → 解析（两种模式，运行时切换）
       ├─ 本地模式：纯 Dart 正则 + 关键词库（离线）
       └─ AI 模式：OpenAI 兼容 LLM API（结构化输出）
  → 待确认卡片列表（逐条可编辑全部字段）
  → 用户同意（点击保存）→ 写入本地 SQLite
  → 日历月视图 / 今日待办列表呈现
```

## 1.3 MVP 范围清单

- 月视图日历 + 今日待办列表（周/日视图为二期）
- 手动创建 / 编辑 / 删除事件（日历点选日期直接新建）
- **主界面底部 AI 对话式输入条**：文本输入 + 图片上传（文件夹选择 / 全窗口拖拽）、附件缩略图可删、图文并存、回车发送（Shift+Enter 换行）、右侧本地/AI 解析模式小分段控件
- 解析结果以**独立面板**呈现，逐条确认 + 全字段可编辑的确认卡片流
- 定时事件 + 全天事件
- 事件编辑表单（标题 / 日期 / 时间 / 全天开关 / 地点 / 备注）
- 设置页：解析模式切换、LLM 配置（baseURL / API Key / model）、测试连接
- 数据互通：ics 导出、JSON 全量备份 / 恢复
- 仿 macOS 桌面应用 UI（浅色 / 深色双主题）
- 本地 git 仓库完整迭代记录

## 1.4 非目标与二期 backlog（MVP 明确不做）

| 项目 | 说明 |
|---|---|
| 移动端（Android/iOS） | 整体二期；本设计的解析器 / 存储层与 UI 解耦，可平移复用 |
| 多端同步服务器 | 不做；数据仅存本机 |
| 重复事件（每周例会等） | 二期，需扩展 rrule 字段与日历渲染 |
| 事件分类标签 / 颜色 | 二期，需扩展 tags 表 |
| Windows 通知提醒 | 二期，需引入 flutter_local_notifications |
| 事件优先级 | 二期 |
| 周视图 / 日视图 | 二期 |
| 推送 GitHub 远程仓库 | 待用户日后决定；保持纯本地 git，见第 13 章 |

## 1.5 系统要求

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10 21H2+ / Windows 11，x64 |
| OCR 前置 | 无系统依赖：RapidOCR 模型随应用打包，本地离线推理，无需 OCR 语言包或 MSIX 包身份 |
| 运行时 | 随应用分发 Flutter 运行时（release 构建自带），无需用户安装 |
| 网络 | 非必需；仅 AI 解析模式需要访问所配置的 LLM API |

## 1.6 术语表

| 术语 | 含义 |
|---|---|
| 事件（Event） | 已确认、已写入数据库的日历条目 |
| 草稿（ParsedEvent） | 解析产物，仅存在于内存的待确认条目，不落库 |
| 缺失标记（missing） | 草稿中缺时间 / 缺地点 / 缺标题的警告标记 |
| 本地模式 | 离线正则 + 关键词解析模式 |
| AI 模式 | 调用 OpenAI 兼容 LLM 的解析模式 |
| 确认（保存） | 用户点击保存、草稿转为事件的唯一动作 |

---

# 2. 技术选型与理由

## 2.1 框架：Flutter（Windows 桌面）

- 单一 Dart 代码库，官方支持 Windows 桌面 target，产物为原生 exe（release 构建自带 Dart AOT 运行时）。
- 自绘渲染引擎（Skia/Impeller），不依赖系统控件，可完全实现 macOS 风格的自定义视觉，无需与 WinUI 样式搏斗。
- 二期平移移动端时 UI 层可整体保留（鼠标 hover 等桌面交互需按平台适配）。

## 2.2 依赖清单与用途

| 包 | 用途 | 版本策略 |
|---|---|---|
| `flutter_riverpod` | 状态管理（providers / 用例层） | pub.dev 当前稳定版，caret 约束 + 提交 `pubspec.lock` 锁定 |
| `drift` + `drift_dev` + `build_runner` | SQLite ORM：类型安全查询、流式数据、schema 迁移 | 同上 |
| `dio` | HTTP 客户端（LLM API） | 同上 |
| `flutter_onnx_ocr` | **RapidOCR 本地 OCR 引擎**（ONNX Runtime + PaddleOCR PP-OCRv5 模型，纯 Dart 预处理/后处理，FFI 插件）；Windows/macOS 已验证，Android/iOS 随底层 onnxruntime_v2 全平台支持扩展；**非 WinRT API，无需 MSIX 包身份** | 模型随 assets 打包（约 20MB） |
| `onnxruntime_v2` | ONNX Runtime 推理后端（Android/iOS/macOS/Windows/Linux 全平台） | flutter_onnx_ocr 传递依赖 |
| `desktop_drop` | 全窗口图片拖拽上传（Windows 支持；DropTarget 包裹应用壳，回调返回文件路径列表） | 同上；若不可用启用 B 计划（WM_DROPFILES FFI 自封装） |
| `window_manager` | 无边框窗口、自绘标题栏、窗口控制 | 同上 |
| `path_provider` | 获取 `%APPDATA%` 等系统路径 | 同上 |
| `file_picker` | 选择图片 / 文本文件 | 同上 |
| `pasteboard` | 读取剪贴板文本与图片 | 同上 |
| `intl` | 日期格式化（中文） | 同上 |
| `flutter_lints`（dev） | 代码规范 | 同上 |

版本策略：应用类项目**提交 `pubspec.lock`** 保证可复现构建；升级依赖前必须跑全量测试（第 15 章），升级动作单独成 commit（`chore(deps): ...`）。

## 2.3 明确不引入的组件

- 不使用 Material / Cupertino 组件库做业务 UI：两者自带视觉语言与"仿 macOS"目标冲突，全部视觉组件自建（`lib/shared/design`，见第 9 章）。
- 不引入第三方 NLP 库（Dart 生态无成熟 jieba 移植）：离线解析用纯 Dart 正则 + 词库实现（第 5 章），保持零依赖、离线可控。
- 不打包 SF 字体、苹果图标等版权资产（第 9.3 节、第 17 章）。

## 2.4 开发环境现状与工具链安装（M0）

已核实的本机环境：

| 项 | 状态 |
|---|---|
| Visual Studio Community 2022（17.14，含"使用 C++ 的桌面开发"工作负载） | ✅ 已就绪（`E:\Microsoft Visual Studio\2022\Community`） |
| Git | ✅ 已就绪（`E:\Git\cmd\git.exe`），全局身份 liubaiqing |
| Flutter SDK | ❌ 未安装 |

**Flutter SDK 安装步骤（M0 执行）：**

1. 克隆 stable 分支到 `E:\Flutter`：`git clone -b stable https://github.com/flutter/flutter.git E:\Flutter`
2. 用户环境变量 PATH 追加 `E:\Flutter\bin`（本机 git 位于 `E:\Git`，克隆前确认网络可达 github.com）。
3. `flutter doctor` 验证：确认 Flutter toolchain 与 "Visual Studio - develop Windows apps" 两项无阻塞错误。
4. `flutter config --enable-windows-desktop`（默认已启用，校验即可）。
5. 在仓库根目录（`calendar/`，即本项目 git 仓库）生成工程：
   `flutter create --platforms=windows --project-name calendar .`
6. 跑通空工程：`flutter run -d windows`，以独立 commit 收尾（`chore: 初始化 Flutter Windows 工程`）。

---

# 3. 系统架构

## 3.1 分层架构

```
┌────────────────────────────────────────────────────┐
│  UI 层（lib/features/*，lib/shared/design）         │
│  页面 / 组件 / 设计系统组件，只消费 provider         │
├────────────────────────────────────────────────────┤
│  application 层（Riverpod providers / 用例）        │
│  状态、流程编排（确认流状态机）、用例函数             │
├────────────────────────────────────────────────────┤
│  domain 层（纯 Dart，零依赖）                       │
│  Event / ParsedEvent / EventParser 接口 / 词表与规则 │
├────────────────────────────────────────────────────┤
│  infrastructure 层                                 │
│  drift DAO │ OCR 服务 │ OpenAI 兼容客户端           │
└────────────────────────────────────────────────────┘
```

依赖方向：上层可依赖下层；domain 不依赖任何 infrastructure 细节；UI 不直接触碰 DAO 与网络客户端。

## 3.2 目录结构

```
calendar/
├── pubspec.yaml
├── windows/                  # Flutter Windows 壳工程
├── lib/
│   ├── main.dart
│   ├── app/
│   │   ├── app.dart          # 根 widget、主题（浅/深色跟随系统）
│   │   └── router.dart       # 页面路由（桌面端简单导航即可）
│   ├── core/
│   │   ├── constants.dart    # 应用名、DB 文件名、默认 LLM 配置
│   │   ├── errors.dart       # 统一错误类型（OcrError/AiError/...）
│   │   └── regex_patterns.dart # 离线解析正则库（唯一出处）
│   ├── shared/design/        # 仿 macOS 设计系统组件（第 9 章）
│   │   ├── ds_button.dart
│   │   ├── ds_card.dart
│   │   ├── ds_text_field.dart
│   │   ├── ds_segmented_control.dart
│   │   ├── ds_dialog.dart
│   │   ├── ds_list_tile.dart
│   │   ├── ds_date_picker.dart
│   │   ├── ds_tooltip.dart
│   │   └── ds_tokens.dart    # 色彩 / 圆角 / 字号 / 动效 token
│   ├── features/
│   │   ├── calendar/         # 月视图 + 今日待办列表
│   │   ├── intake/           # 输入页 + OCR + 解析 + 确认卡片流
│   │   ├── event/            # 事件表单（新建/编辑）
│   │   └── settings/         # 解析模式 / LLM 配置 / 数据管理
│   └── data/
│       ├── db/
│       │   ├── database.dart  # drift 数据库实例
│       │   ├── tables.dart    # events / settings 表定义
│       │   └── event_dao.dart # 事件查询与写入
│       ├── parsers/
│       │   ├── event_parser.dart   # 抽象接口
│       │   ├── local_parser.dart   # 离线正则+词库
│       │   └── ai_parser.dart      # LLM 结构化输出
│       ├── llm/
│       │   └── openai_compatible_client.dart
│       └── ocr/
│           └── ocr_service.dart    # Windows.Media.Ocr 封装
└── test/
    ├── unit/                 # 解析器 / ics / 校验
    ├── widget/               # 组件与页面
    └── integration/          # AiParser（fake 客户端）
```

## 3.3 解析器策略模式（核心设计）

```dart
/// domain 层抽象：两种解析模式实现同一接口，输出统一的草稿模型。
abstract class EventParser {
  /// 输入原文（文本或 OCR 结果），输出结构化草稿列表。
  /// 解析失败抛 [ParserException]，不返回部分结果。
  Future<List<ParsedEvent>> parse(String text);
}

class LocalParser implements EventParser { ... } // 离线，第 5 章
class AiParser implements EventParser { ... }    // 在线，第 6 章
```

- 模式选择：settings 表 `parseMode`（`local` / `ai`），输入页顶部 SegmentedControl 可即时切换。
- 无论哪种模式，输出统一为 `ParsedEvent` 草稿，进入**同一条**确认流程（第 8 章）。
- 解析器依赖注入：AiParser 接收 `OpenAiCompatibleClient` 接口，便于测试注入 fake（第 15.3 节）。

## 3.4 模块依赖规则

- `core/` 与 `shared/design/` 不依赖任何 feature；
- feature 之间的导航用路由，不互相 import widget 内部实现；
- 数据库、OCR、LLM 客户端只允许 application 层调用，UI 层经 provider 取值。

---

# 4. 数据模型与存储

## 4.1 数据库：drift（SQLite）

drift 表定义（`data/db/tables.dart`）：

```dart
enum EventSourceType { text, image, manual } // 0/1/2

class Events extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get title => text()();                        // 非空
  TextColumn get location => text().nullable()();
  DateTimeColumn get start => dateTime()();                // 本地时间；全天事件为当日 00:00
  DateTimeColumn get end => dateTime().nullable()();       // 全天事件为 null
  BoolColumn get allDay => boolean().withDefault(const Constant(false))();
  TextColumn get note => text().nullable()();
  IntColumn get sourceType => intEnum<EventSourceType>()();
  TextColumn get sourceText => text().nullable()();        // 原始文本或 OCR 结果
  DateTimeColumn get createdAt => dateTime().withDefault(currentDateAndTime)();
  DateTimeColumn get updatedAt => dateTime().withDefault(currentDateAndTime)();
  DateTimeColumn get deletedAt => dateTime().nullable()(); // 软删除标记（回收站）：null=正常，非 null=已删除时刻
}

class Settings extends Table {
  TextColumn get key => text()();
  TextColumn get value => text()();
  @override
  Set<Column<Object>> get primaryKey => {key};
}
```

设置项约定：

| key | 默认值 | 说明 |
|---|---|---|
| `parseMode` | `local` | 解析模式；**默认本地模式**（离线可用、无需 API Key） |
| `llmBaseUrl` | `https://api.deepseek.com` | OpenAI 兼容端点 |
| `llmApiKey` | （空） | 用户自行填写 |
| `llmModel` | `deepseek-chat` | 模型名 |
| `themeMode` | `light` | 主题三选一：`light`（浅色）/ `dark`（深色）/ `glass`（毛玻璃，基于浅色）；设置页修改后持久化并即时生效 |

**回收站采用软删除（`deletedAt` 字段），不建独立回收站表**：删除 = 写 `deletedAt = now`；恢复 = 置 null；彻底清理 = 物理 DELETE。理由：现有查询（月查询/日查询/ics 导出/JSON 备份）统一追加 `deletedAt IS NULL` 过滤即可；避免副本表带来的 id 变化与数据双份；`events` 表仍是唯一事件源。UI 层所有正常事件查询与导出必须带该过滤条件，回收站查询条件为 `deletedAt IS NOT NULL`（按 `deletedAt` 倒序）。

## 4.2 草稿模型（仅内存，不落库）

```dart
enum MissingField { time, location, title }

class ParsedEvent {
  final String title;            // 可能为空字符串（缺失标题）
  final String? location;
  final DateTime? start;         // null 表示未识别出时间
  final DateTime? end;
  final bool allDay;
  final String? note;
  final EventSourceType sourceType;
  final String sourceText;       // 该草稿对应的原始文本
  final Set<MissingField> missing; // UI 据此红色提示
  final double confidence;       // 0.0~1.0，本地模式按规则命中打分

  /// 转换为可入库的 Event（由确认卡片调用）
  Event toEvent() { ... }
}
```

## 4.3 写入原则（"同意才写入"）

- 解析产物**只存在于内存**（草稿），数据库中没有 draft 状态，因此 `events` 表不设 status 字段——库中所有记录均为用户已确认的事件。
- 用户在确认卡片点击"保存"是唯一写库路径；写入失败时草稿保留在 UI 并 toast 报错（第 14 章）。
- 手动创建的事件（`sourceType == manual`）走表单 → 保存 → 直接入库，`sourceText` 为 null。

## 4.4 存储路径与迁移

- 数据库文件：`%APPDATA%\calendar\calendar.sqlite`（`path_provider` 的 `getApplicationSupportDirectory()`）。
- drift 迁移：`schemaVersion` 从 1 起；任何表结构变更必须新增 MigrationStrategy 步骤并升版本号，同时补迁移测试。禁止直接改旧 schema。
- **v1.3.2 迁移（新增 `deletedAt` 列）**：`ALTER TABLE events ADD COLUMN deleted_at`（nullable，老数据全部为 null，行为不变）；迁移测试需验证升级后正常事件查询、月视图、ics 导出均不受影响。

## 4.5 API Key 存储风险（已接受，二期改进）

MVP 中 `llmApiKey` 以明文存于本地 SQLite。风险与缓解：

- 数据库文件已通过 `.gitignore` 排除（第 13.6 节），不会进入版本库；
- JSON 备份不含 settings（第 12.2 节），Key 不会随备份扩散；
- 二期改为 Windows DPAPI（`flutter_secure_storage` 或直接调 CryptProtectData）加密存储，迁移时自动搬运旧值。

---

# 5. 离线解析引擎（LocalParser）

纯 Dart 实现，零网络、零第三方 NLP 依赖。所有正则集中在 `core/regex_patterns.dart`，词表集中在 `domain` 层常量文件中，便于单独测试与迭代。

## 5.1 处理管道

```
原始文本
  → 1) 句子切分（换行 / 分号 / 句号 / 感叹号）
  → 2) 每句依次执行：日期时间抽取 → 地点抽取 → 事务识别
  → 3) 组装 ParsedEvent（含 missing 标记与置信度）
  → 4) 全句零命中 → 该句丢弃；全部零命中 → 抛 ParserException
```

## 5.2 中文日期时间抽取与归一化规则

**绝对日期**：

| 输入示例 | 归一化规则 |
|---|---|
| `2025年3月12日` / `2025-03-12` / `2025/3/12` | 直接解析 |
| `3月12日` / `3-12`（无年份） | **默认当前系统年份**；已过日期保留（UI 显示"日期已过"提示），不自动顺延 |
| `15号` / `15日`（仅提供日） | **年月默认当前系统时间**；当月无此日（如 4月31日）视为无效 |

**相对日期**：

| 输入 | 规则 |
|---|---|
| 今天 / 明天 / 后天 / 大后天 | 当前日期 +0/1/2/3 天 |
| 昨天 / 前天 | 解析为过去日期，草稿保留但 UI 显示"日期已过"提示，允许保存（不强制拦截） |
| 本周X | 以周一为一周之始，取本周对应星期 |
| 下周X | 严格取下一周的对应星期 |
| 周X（无本/下） | 若本周该日 ≥ 今天则取本周，否则取下周（就近未来） |
| 周末 | 取本周六；若已过则下周六 |

**时间**：

| 输入示例 | 归一化 |
|---|---|
| `上午9点` / `9点` / `9:00` | 9:00 |
| `下午3点半` / `15:30` | 15:30 |
| `晚上8点` | 20:00 |

时段映射：凌晨 0–5、早上 5–9、上午 9–12、中午 12–13（`12点` 一律归中午 12:00）、下午 13–18、傍晚 18–19、晚上 19–24。同一时刻出现矛盾表述（如"下午12点"）以数值为准并降置信度。

**区间**：`X点到Y点`、`X点-Y点`、`X:00-Y:30` → start/end 分别填充。

**缺省规则**：

- 有日期无时刻 → `allDay = true`，start 为当日 00:00，end 为 null；
- 有时刻无日期 → 默认今天（时刻已过则顺延明天，就近未来），`allDay = false`；
- 单一时刻（无区间）→ end 为 null，日历上呈现为时间点条目；
- 结束 < 开始（如跨天"晚上11点到凌晨1点"）→ end 加一天。

**边界情况**：跨月"下周X"、闰年 2 月 29、无年份固定当前年（已过提示不顺延）、周一起始的星期计算——均须有单元测试用例（第 15.1 节）。**已知限制**（写入文档、UI 不承诺）：中文数字日期（"三月十二日"）、"月底/月初"、"五一/国庆"节假日别名不在 MVP 规则内，此类文本建议 AI 模式。

## 5.3 地点抽取

两级策略：

1. **介词语境**：以 `去/到/在/前往/飞往/抵达/出差至/在...` 为锚点，向后贪婪取名词短语（至标点、时间词或动词为止），长度 2–20 字。
2. **后缀词表兜底**：无介词命中时，扫描后缀词表，命中后向前取 2–8 字构成地点。

地点后缀词表（初始清单，见附录 A）：市 / 省 / 区 / 县 / 镇 / 路 / 街 / 道 / 巷 / 大厦 / 大楼 / 广场 / 中心 / 酒店 / 饭店 / 机场 / 火车站 / 高铁站 / 地铁站 / 车站 / 码头 / 港口 / 园区 / 科技园 / 学校 / 大学 / 学院 / 医院 / 银行 / 公司 / 集团 / 总部 / 分部 / 门店 / 会议室 / 办公室 / 体育馆 / 图书馆 / 公园 / 景区。

## 5.4 事务识别

动词 / 名词关键词表（初始清单，附录 A）：出差 / 开会 / 会议 / 例会 / 面试 / 考试 / 测验 / 体检 / 聚会 / 聚餐 / 拜访 / 约见 / 签约 / 培训 / 上课 / 讲座 / 报告 / 答辩 / 值班 / 搬家 / 婚礼 / 生日 / 纪念日 / 约会 / 团建 / 旅游 / 出发 / 回国 / 返程 / 注册 / 报名 / 提交 / 截止 / 还款 / 缴费 / 续费。

标题生成规则（按优先级）：

1. 命中"动词 + 宾语"结构 → 标题取该动宾短语（例："去上海出差开会" → "出差开会"）；
2. 仅命中关键词 → 标题取关键词；
3. 未命中 → 取去除时间、地点片段后的句子主干（≤ 20 字），且 `missing` 不标记 title（有主干即视为有标题）。

## 5.5 多事件切分

- 每句独立解析，一句一个草稿；
- 一句中出现多个"时间 + 动词"对时，按动词位置二次切分；
- 不做跨句合并（MVP 简化，文档注明）；
- **标题/引用行过滤**：以【开头且不含任何时间信息的句子视为文档标题（如"【通知2】…培训通知"），不产生事件；
- **关键词排除后缀**："会议纪要/会议记录/议程"中的"会议"为复合名词，不视为事务动词。

## 5.6 置信度与缺失标记

- 打分：时间命中 +0.4、地点命中 +0.3、动词/关键词命中 +0.3；confidence < 0.5 为低置信。
- `missing`：start 为 null → time；location 为 null → location；标题为空 → title。
- 确认卡片上：低置信显示"建议人工核对"徽标；missing 字段以红色边框/提示呈现。

## 5.7 用例示例（同时作为单元测试基准）

| 输入 | 期望输出 |
|---|---|
| `3月15日下午2点去上海虹桥机场出差` | title=出差，location=上海虹桥机场，start=今年3月15日 14:00，allDay=false |
| `明天上午10点在公司3楼会议室开会，讨论季度计划` | title=开会，location=公司3楼会议室，start=明天 10:00，note=讨论季度计划 |
| `下周三去杭州见客户` | 日期=下周三（全天），location=杭州，title=见客户 |
| `12月25日 圣诞节` | 12月25日全天，title=圣诞节，missing={location} |
| `记得交房租` | 无时间 → missing={time}，title=交房租，低置信 |

---

# 6. 在线解析引擎（AiParser）

## 6.1 协议与端点

- OpenAI 兼容 Chat Completions：`POST {baseUrl}/chat/completions`；
- 默认配置：baseUrl `https://api.deepseek.com`，model `deepseek-chat`（已核实其支持 JSON Output 模式，见附录 C）；
- 请求头：`Authorization: Bearer {key}`，`Content-Type: application/json`。

## 6.2 Prompt 与结构化输出

请求体要点：

```json
{
  "model": "deepseek-chat",
  "temperature": 0.1,
  "max_tokens": 2000,
  "response_format": { "type": "json_object" },
  "messages": [
    {
      "role": "system",
      "content": "你是日历信息抽取助手。从用户文本中抽取全部事件，严格按以下 JSON 格式输出：{\"events\":[{\"title\":\"字符串，事件标题\",\"location\":\"地点或null\",\"start\":\"ISO8601本地时间如2025-03-12T09:00:00，全天事件用当天00:00:00；无法确定时null\",\"end\":\"ISO8601或null\",\"all_day\":\"布尔，无具体时刻为true\",\"note\":\"补充信息或null\"}]}。规则：1)时间不确定时start为null而不是猜测；2)不输出任何JSON以外的文字；3)地点只取地名，不包含动词。"
    },
    { "role": "user", "content": "<用户原文>" }
  ]
}
```

返回 JSON Schema 校验（客户端本地校验，不信任模型）：

```json
{
  "type": "object",
  "properties": {
    "events": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "title": { "type": "string" },
          "location": { "type": ["string", "null"] },
          "start": { "type": ["string", "null"] },
          "end": { "type": ["string", "null"] },
          "all_day": { "type": "boolean" },
          "note": { "type": ["string", "null"] }
        },
        "required": ["title", "location", "start", "end", "all_day", "note"]
      }
    }
  },
  "required": ["events"]
}
```

校验与转换规则：

- title 为空串 → 该条丢弃并计入警告提示；
- start 为 null 且 all_day=false → `missing += time`；start 解析失败同样处理；
- all_day=true → 强制 start 归零时刻（00:00）、end=null；
- **年份/月份缺省规则（与本地模式一致）**：system prompt 注入当前年月并规定"用户未指定年份时年份用当前年；仅提供'日'时年月用当前年月"；客户端兜底——① 用户文本无显式年份（`\d{4}年/-`）且 AI 返回年份 ≠ 当前年 → 强制替换为当前年（保留月日时分）；② AI 返回缺年份格式（`M-d` / `M-dTH:m`）→ 宽松解析补当前年；③ 仅返回日（`15` / `15T14:00`）→ 补当前年月（含当月天数校验）；
- end 存在且 < start → end 加一天（与本地模式一致）；
- 整体返回非法 JSON / 字段缺失 → 抛 ParserException，UI 提示并建议切换本地模式。

## 6.3 网络参数与重试

- dio：连接超时 10s、接收超时 30s；
- 仅对网络错误、5xx、429 自动重试 1 次，间隔 2s；4xx（含 401）不重试；
- `OpenAiCompatibleClient` 为接口，`AiParser` 依赖接口（便于 fake 注入测试）。

## 6.4 错误处理

| 现象 | 用户提示 | 处理 |
|---|---|---|
| 401 | "API Key 无效，请到设置页检查" | 不重试，跳转设置页按钮 |
| 402/429 | "额度不足或请求过于频繁，请稍后再试" | 429 重试 1 次 |
| 超时 / 网络错误 | "网络异常，请检查连接；或切换本地模式解析" | 重试 1 次后报错 |
| JSON 非法 / 校验失败 | "AI 返回格式异常，建议切换本地模式重试" | 抛 ParserException |

## 6.5 与本地模式的关系

- 输出统一为 `ParsedEvent`，共用确认卡片流程，互为主备；
- 设置页提供"测试连接"：`GET {baseUrl}/models` 带 Key 请求，200 即视为连通。

---

# 7. OCR 模块（图片输入）

## 7.1 引擎：RapidOCR（本地离线，跨平台）

- **RapidOCR**（ONNX Runtime + PaddleOCR PP-OCRv5 模型），经 `flutter_onnx_ocr` FFI 插件接入；预处理/后处理为纯 Dart，推理走 `onnxruntime_v2`（Android/iOS/macOS/Windows/Linux 全平台后端）。
- **优点**：一套模型全平台复用（少维护）；本地离线；中文质量高（PP-OCRv5）；**非 WinRT API，无需 MSIX 包身份**，普通 exe 即可运行。
- 模型资产（随应用打包，约 20MB）：`ch_PP-OCRv5_det_mobile.onnx`（检测）、`ch_PP-OCRv5_rec_mobile.onnx`（识别）、`ch_ppocrv5_dict.txt`（多语言字典，含简繁中/英/日）。
- 初始化：懒加载（首次识别时加载模型约 1–2 秒），失败允许重试。
- 当前平台状态：Windows/macOS 已验证；Android/iOS 待底层支持扩展（onnxruntime_v2 已全平台，扩展成本低）。

## 7.2 处理流程与预处理

```
图片（png/jpg/bmp）
  → 解码为 ui.Image
  → 最长边 > 2000px 时等比缩放至 2000px（避免引擎输入尺寸超限）
  → RapidOCR 识别（检测框外扩 + 动态输入宽度 ≤2048，小图友好）
  → 后处理：按阅读顺序排序；y 中心相近的检测框合并为同一行；
           折行合并（宽高比 ≥8 的长行且非句末标点 → 与下一行拼接，恢复被排版换行截断的完整句）
  → 返回纯文本（逐行），供输入条附件流程合并解析
```

## 7.3 引擎可替换性

`OcrService` 为唯一接入点（接口不变、实现可替换）：若 RapidOCR 在特定平台不可用，可换回各平台原生引擎（Android ML Kit / iOS Vision / Windows.Media.Ocr），调用方零改动（文档 §3.4 分层约束）。

## 7.4 图片输入途径

1. `file_picker` 选择本地图片文件；
2. 全窗口拖拽图片（`desktop_drop`）；
3. 剪贴板粘贴图片（Ctrl+V，`pasteboard`）。

三者统一汇入输入条附件流程。`sourceType = image`，OCR 结果与手输文本合并后解析。

---

# 8. 确认流程与输入页（核心交互状态机）

## 8.1 状态机

```
idle ──输入文本/图片──▶ inputReady
inputReady ──(图片)──▶ ocrRunning ──成功──▶ textReady
                                     └─失败──▶ ocrFailed ──重试/换图──▶ inputReady
inputReady/textReady ──点"解析"──▶ parsing
parsing ──成功──▶ cardsReady（草稿卡片列表）
parsing ──失败──▶ parseFailed ──错误提示（可切模式/改文本）──▶ textReady
cardsReady ──单条"保存"──▶ 写入 DB 成功 → 该卡片变为"已保存"态
                     └─ 写入失败 → toast，卡片保留可重试
cardsReady ──单条"删除"──▶ 丢弃该草稿（不写库）
cardsReady ──"全部保存"──▶ 逐条写入，已保存/失败分别汇总提示
全部卡片处理完毕 ──▶ 跳转日历视图（对应日期高亮可见）
```

## 8.2 界面结构

- **输入入口（日历主界面底部，ChatComposerBar）**：
  - 胶囊形圆角输入条（圆角 22）；左侧上传按钮（文件夹选择图片，支持多张）、中间多行文本输入（placeholder"输入包含时间、地点、事务的内容，或上传图片…"）、右侧解析模式小分段控件（本地|AI，选择持久化到 settings.parseMode）与圆形发送按钮；
  - 回车发送、Shift+Enter 换行；发送/解析中按钮转加载态并禁用输入；
  - **图片附件**：上传或**全窗口拖拽**（desktop_drop）进入输入条，以 64px 缩略图展示（可删除、可补充文字，图文并存）；**Ctrl+V 粘贴**：剪贴板为图片时直接作为附件（临时文件随附件删除清理），为文本时正常粘贴进输入框；点发送才执行 OCR+解析；
  - 失败提示显示在输入条上方（红色小字）。
- **结果面板（解析成功后弹出，宽 680 / 最大高 80% 视口 / 圆角 14）**：
  - 标题"待确认事件（N 条未保存）" + 关闭按钮；
  - 草稿卡片列表：每张卡片含——
    - 字段编辑：标题（文本框）、日期（DSDatePicker）、时间 / 结束时间（时间选择器）、全天开关、地点、备注；
    - 缺失字段红色高亮 + 低置信"建议人工核对"徽标；
    - 操作按钮：保存（primary）、删除（destructive）；已保存卡片置灰并打勾，可撤销（删除该已写入事件，弹确认框）；
  - 底部"全部保存"按钮；关闭时若有未保存草稿 → 弹确认"关闭将丢弃 N 条草稿"；
  - 保存后日历流式数据自动刷新。
- 空结果提示：解析零命中时提示"未能识别出事件信息"，可手动新建（今日列表"＋"按钮）。

## 8.3 字段校验（保存时）

- 标题非空（trim 后）；缺失标题的卡片保存时拦截并聚焦该字段；
- start 必填（AI 模式可能为 null，拦截提示补日期）；
- end 非空且 < start → 错误提示；
- allDay=true 时隐藏时刻控件、end 置 null。

## 8.4 失败分支

- 离线解析零命中 → 提示"未能识别出事件信息，可直接手动新建"；
- OCR 失败 → 错误说明 + 重试按钮；
- AI 失败 → 第 6.4 节错误文案 + "切换本地模式"快捷操作。

---

# 9. UI/UX 设计规范（仿 macOS 桌面应用）

目标观感：macOS 原生应用（参考 Fantastical / 系统日历）：低对比灰阶背景、蓝色强调色、侧边栏分区、精致卡片与柔和阴影。

## 9.1 窗口与标题栏

- `window_manager` 无边框窗口；默认尺寸 1100×720，最小 900×600，记忆上次尺寸与位置（settings 表）。
- **窗口圆角**：启动时经 `DwmSetWindowAttribute` 设置 `DWMWA_WINDOW_CORNER_PREFERENCE=33 / DWMWCP_ROUND=2`（Windows 11 原生 DWM 圆角）；最大化时系统自动切回直角；非 Win11 环境静默降级为直角。
- 自绘标题栏（高 38）：左侧应用名小字；**右上角 Windows 风格窗口按钮**（标准 46×38 命中区）——最小化 `—`、最大化 `□`（最大化状态下显示还原 `▣`，经 WindowListener 同步状态）、关闭 `×`；hover 提亮背景，关闭按钮 hover 红底 `#E81123` 白字（Windows 惯例）。
- **交互即时性约束（重要）**：按钮区与"双击最大化"手势区**分离**——`onTap` 与 `onDoubleTap` 同处一个手势区时，单击会被延迟约 300ms（双击判定超时，框架常量不可配置）。因此：标题栏左侧空白区承载拖拽（`startDragging`）与双击最大化，右上角按钮区为独立手势区，点击即时响应。

## 9.2 布局

```
┌────────────────────────────────────────────┐
│ ● ● ●        calendar                     │  标题栏 38px
├────────────────────────────────────────────┤
│           月视图（自绘网格）                 │
│  ◀ 2026年3月 ▶ [今天]    ┌─────────────────┐│
│                         │ 今日待办列表 320px││
│     月历网格（主区占满）  │                 ││
│                         │  ● ← 设置圆钮     ││
│  ┌───────────────────────────────────┐     │
│  │  ⚪ 输入包含时间、地点、事务… 本地|AI ↑ │  │
│  └───────────────────────────────────┘     │
└────────────────────────────────────────────┘
```

- **无侧边栏**：日历占满整个内容区；设置入口为**左下角圆形按钮**（40px，hover 提亮），点击弹出**设置小窗**（宽 560 / 圆角 14，见第 11 章）。
- 主区比例：月历自适应占满，右侧列表固定 320px（窗口过窄时折叠为底部列表，MVP 阈值 1000px 以下自动折叠）。
- **主界面底部：AI 对话式输入条（ChatComposerBar，胶囊圆角 22），居中且长度受限（maxWidth 640）**，不遮挡内容区。

## 9.3 字体

- **不打包 SF 字体**（版权原因，见第 17 章）；字体策略：**打包 HarmonyOS Sans SC（华为开源字体，免费商用，Latin+简体中文完整字形）作为唯一主字体**——一款字体覆盖中英文，彻底消除混排割裂；回退链保留微软雅黑兜底（生僻字）。
- **字重规范：只允许 400（正文）与 700（标题/强调）两个真实字重**；**禁止 500/600**——引擎会合成加粗（fake bold），中文下表现为发虚发胖。仅打包 Regular/Bold 两个字重文件（约 15.7MB）。
- 许可合规：HarmonyOS Sans 许可要求软件中显著声明字体使用（设置页"关于"已声明），不得修改字体文件；LICENSE 随包分发。
- 字号梯度：标题 20/17、正文 13、辅助 12、小字 11；行高 1.5；数字/时间对齐场景用固定宽度容器（不依赖等宽字体）。

## 9.4 色彩规范（token，集中于 `ds_tokens.dart`）

| Token | 浅色 | 深色 |
|---|---|---|
| 侧边栏背景 | `#F5F5F7`（毛玻璃下 80% 不透明度） | `#1C1C1E` |
| 主区背景 | `#FFFFFF` | `#1E1E1E` |
| 卡片背景 | `#FFFFFF` | `#2C2C2E` |
| 主文字 | `rgba(0,0,0,0.85)` | `rgba(255,255,255,0.92)` |
| 次文字 | `rgba(0,0,0,0.5)` | `rgba(255,255,255,0.55)` |
| 分隔线 | `rgba(0,0,0,0.1)` | `rgba(255,255,255,0.1)` |
| 强调蓝 | `#007AFF` | `#0A84FF` |
| 危险红 | `#FF3B30` | `#FF453A` |
| 成功绿 | `#34C759` | `#32D74B` |
| 警示橙 | `#FF9500` | `#FF9F0A` |
| 黄 | `#FFCC00` | `#FFD60A` |

今日日期：蓝底白字圆形。**主题由设置页三选一显式控制**（`themeMode` key：浅色/深色/毛玻璃，默认浅色），**不再跟随系统亮度**；主题切换即时生效并持久化，重启沿用。

### 9.4.1 深色主题规范（Apple HIG Dark Mode）

依据 [Apple HIG Dark Mode 官方规范](https://developer.apple.com/design/human-interface-guidelines/dark-mode)，现有深色 token 已基本达标，设计原则显式化如下：

- **不纯黑**：背景用深灰 `#1E1E1E`（纯黑会与高对比内容冲突并产生光晕）；
- **亮度分层代替阴影**（深色下投影不可见，HIG 核心原则）：主背景 `#1E1E1E` → 卡片 `#2C2C2E` → 弹层 `#3A3A3C` 逐级变亮；层间用 hairline 描边 `rgba(255,255,255,0.08)` 分界；**深色主题移除卡片/弹层投影**（§9.5 阴影规则同步）；
- **强调色自动提亮**：蓝 `#0A84FF`、红 `#FF453A`、绿 `#32D74B`、橙 `#FF9F0A`、黄 `#FFD60A`（HIG 深色系统色板，现有值已符合）；
- **文字对比度**：正文主文字 `rgba(255,255,255,0.92)` on `#1E1E1E` ≈ 15:1，次文字 0.55 亦满足 WCAG AA（≥4.5:1）；
- **避免大面积纯白块**：卡片、输入框一律深灰底白字，不使用白色面板。

### 9.4.2 毛玻璃主题材质（Apple HIG Materials）

毛玻璃为第三主题（`themeMode = glass`），基于**浅色**色板实现，质感依据 [Apple HIG Materials 官方规范](https://human-interface-guidelines.gitbook.io/human-interface-guidelines/foundations/materials)：

- **原理**：背景内容经高斯模糊 + 半透明白色着色 + 饱和度补偿后透出，形成玻璃质感与空间层次；
- **官方参数映射**（macOS `NSVisualEffectView` 惯例 → Flutter `BackdropFilter`）：

| 参数 | 值 |
|---|---|
| 模糊 | `ImageFilter.blur(sigma ≈ 20)` |
| 饱和度补偿 | `ColorFilter.matrix` 饱和度 ≈1.8（毛玻璃会稀释背景色彩，需补偿；仅增强有彩色，中性文字不受影响） |
| 反光渐变着色 | `LinearGradient` 左上高光白 0.4 → 右下透明 0.1（模拟玻璃反光） |
| 边缘渐变描边 | CustomPaint 沿圆角描 1.5px 渐变（白 0.5 → 白 0.12，模拟边缘反射） |
| hairline 描边 | `rgba(0,0,0,0.06)` 1px |
| 阴影 | `0 1 24 rgba(0,0,0,0.15)`，`spreadRadius: -1`（贴合玻璃边缘） |

> 实现参考：[devgex 毛玻璃技术解析](https://devgex.com/zh-CN/article/00042834)（ClipRect 裁剪 + 模糊分层）、[掘金 高斯模糊毛玻璃特效](https://juejin.cn/post/7425527459683860491)（渐变着色/渐变边框/阴影参数）。

- **层次递进**（离用户越近越实，HIG 层次原则）：

| 层 | 规格 |
|---|---|
| 窗口主界面 | **实色背景**（极浅白渐变 0.92→0.78 提供微妙层次；应用内无桌面内容可透，不做整窗模糊）；日历内容清晰 |
| 卡片/输入条 | 白 0.75 + blur 20（输入条、设置分区卡片均接 DSGlassSurface，模糊背后真实内容） |
| 弹层（设置/解析面板） | 白 0.85 + blur 24 + hairline |

> HIG 原则：毛玻璃用于**浮层**（模糊背后真实内容），窗口主背景保持实色。

- **token 扩展**：`DSTokens` 新增 `glassSurface`（半透明白着色）、`glassBorder`（hairline）、`glassBlurSigma`（double）、`glassTintStart/End`（反光渐变）、`glassEdgeStart/End`（边缘描边渐变）；毛玻璃 token 集为基于 `light` 派生的 `DSTokens.glass`——文字/强调色与浅色一致，仅背景/描边/模糊不同；
- 实现走**应用内 `BackdropFilter`**（Flutter 桌面跨平台方案）；Windows DWM Mica/Acrylic 原生窗口透明为二期优化（备注）。

## 9.5 材质与动效

- 侧边栏毛玻璃：`BackdropFilter`（`ImageFilter.blur`），注意性能：仅侧边栏与弹层遮罩使用，避免整页模糊。
- **毛玻璃主题（§9.4.2）应用范围**：仅窗口主背景、卡片/输入条、弹层三处（HIG 层级 ≤3）；**禁止**在月视图网格、列表行等高频重绘区域套 `BackdropFilter`（性能）。
- **深色主题阴影规则（§9.4.1）**：卡片/弹层投影移除，改 hairline 描边 `rgba(255,255,255,0.08)` + 亮度分层；浅色主题阴影维持现状（下表）。
- 圆角：输入条胶囊 22、面板 14、卡片 12、输入框 8、按钮 6、日期格 hover 8；一律走 ds_tokens 常量（kRadiusComposer/kRadiusPanel/kRadiusCard/kRadiusTextField/kRadiusButton）。
- 阴影（浅色主题）：卡片静止 `0 1 3 rgba(0,0,0,0.08)`，hover `0 2 8 rgba(0,0,0,0.12)`；毛玻璃弹层 shadow 调轻（玻璃自身已有层次）。
- 动效：过渡 150–200ms（ease-out）；分段控件滑动 150ms；弹层出现缩放 0.97→1 + 淡入 120ms；hover 高亮即时（桌面端）；**主题切换过渡 200ms 淡入淡出（受动画总开关控制，关闭时瞬时切换）**。

## 9.6 设计系统组件清单（自建，禁止直接使用 Material/Cupertino 组件）

| 组件 | 规格 |
|---|---|
| DSButton | primary 蓝底白字 / default 浅灰底 / destructive 红底；高 32（标准）、28（小）；hover 提亮 5%，按下再降 5%；disabled 40% 透明；圆角 6 |
| DSCard | 圆角 12，阴影见 9.5；可带标题栏、关闭按钮 |
| DSTextField | 圆角 8、边框 `rgba(0,0,0,0.15)`；focus 蓝边 2px + 外发光 `rgba(0,122,255,0.25)`；错误态红边 + 红字提示 |
| DSSegmentedControl | 容器灰底圆角 8；选中块白底 + 阴影，切换时**平滑滑动**（AnimatedPositioned，easeInOutCubicEmphasized 150ms，受动画总开关控制）；**内容自适应**布局（TextPainter 测量最宽文字定段宽），不依赖父宽度约束 |
| DSDialog | 遮罩 `rgba(0,0,0,0.32)`；宽 ≤ 420、圆角 14；标题 + 正文 + 右下按钮（次要 + 主要） |
| DSListTile | 高 40，hover 灰底，前置图标 18，副文字 12 |
| DSDatePicker | 自绘月历弹层（复用月视图网格逻辑），带年月切换 |
| DSDropdown | 点击弹出锚定菜单：hover 高亮、选中打勾、subtitle 展示辅助信息（如 baseURL）；用于供应商选择等 |
| DSYearPicker | 年份选择弹层：12 年一页网格（4×3）+ 上下翻页，hover 高亮、当前年 accentBlue 边框强调；供月视图年月标题跨年跳转 |
| DSGlassSurface | 毛玻璃容器：`BackdropFilter`（blur 20 + 饱和度 1.8）+ 半透明白着色 + hairline 描边；仅毛玻璃主题启用（浅色/深色主题下退化为普通 Container） |
| ChatComposerBar | 主界面底部输入条：胶囊圆角、上传按钮、附件缩略图（可删）、多行输入、模式小分段、圆形发送按钮；回车发送 / Shift+Enter 换行 |
| 解析结果面板 | 独立弹层（宽 680 / 圆角 14）：待确认卡片流（ConfirmCard）+ 全部保存 + 关闭（未保存草稿需确认） |
| DSCheckbox | 勾选框：未选中空心圆角方框、选中蓝底白勾；用于批量删除窗口逐条勾选与全选 |
| 批量删除窗口 | 小窗（宽 560 / 圆角 14）：月份切换 + 事件行列表（日期/时间/标题/地点）+ 逐条勾选 + 全选 + 已选计数 + 删除（确认后软删除进回收站）/ 取消；空态提示（§10.5） |
| 回收站窗口 | 小窗（宽 560 / 圆角 14）：软删除事件列表（按删除时间倒序）+ hover 恢复 + 彻底清理（确认警告后物理删除）+ 空态提示（§10.6） |
| DSTooltip | 深灰底白字、圆角 6、延迟 400ms |

---

# 10. 日历模块

## 10.1 月视图规格

- 7 列网格（**周一起始**，表头 一/二/三/四/五/六/日），5–6 行，单元格最小高 88；
- 星期表头：12px 深色加粗（textPrimary + w600），表头下方 1px 分隔线，与日期区形成明显层次分界；
- 上月/下月补白日期 50% 透明度；跨月日期可点击（切换月份并选中）；
- 今日：蓝底白字圆（直径 26）；选中日：浅蓝底圆；
- 有事件日期：日期数字下方蓝色圆点（直径 4，当天事件 1–3 个显示相应点数，>3 显示 3 点）；
- 顶栏：`2025年3月` 大字标题 + 左/右翻页箭头 + "跳回今天"按钮；**年月标题 hover 浮现淡色圆角底色（提示可点击，无边框线），点击弹出年份选择器（12 年一页网格 + 上下翻页，当前年 accentBlue 强调），选中后保持当前月份直接跨年跳转**；
- 交互：单击选中日期（**即时响应**，日期格不注册 onDoubleTap——单击/双击并存会让单击延迟 300ms；新建事件入口为今日列表"＋"按钮，文档 10.2 节）；键盘 ←/→ 翻月（MVP 可选实现）。
- **选中聚焦动画**：网格上叠加浅蓝选中圆动画层，**全程只有一个聚焦点**（动画期间目标格静态选中圆隐藏，动画圆到位后由静态态接管）——首次点击且本月含今天：圆从今天位置**弹动**至点击处（位置用无 overshoot 的 easeOutCubic，弹动感由缩放弹性承担，避免末段回弹的"定位晃动"观感）；本月不含今天：圆在点击处**原地浮现弹动**（缩放 0.6→1 elasticOut + 淡入）；后续点击：圆从上一位置**丝滑移动**（Apple 风格 `easeInOutCubicEmphasized`）至新位置；**跨月点击（补白格/翻页/今天按钮）：先切换月份，再在新网格中按首次规则播放动画**；重复点击同一天不重复动画。
- 自绘说明：使用自定义 painter 绘制网格与圆点，不依赖任何日历库（保证视觉可控）。

## 10.2 今日待办列表（右侧）

- 标题：`3月12日 星期三`（`intl` 中文格式化）+ 右侧"＋"新建按钮（预填选中日期，文档 10.3 节表单）；
- 分区：全天事件区（显示"全天"标签）→ 定时事件区（按 start 升序）；单时刻事件显示 `HH:mm`，不占时长块；
- 每条：时间 / 标题 / 地点小字（无地点省略）/ 来源图标（文本/图片/手动，tooltip 说明）；
- hover 浮现编辑、删除按钮；删除须经 DSDialog 确认；
- 空态：居中图标 + "这一天还没有安排"。

## 10.3 事件表单与校验

- 字段：标题（必填）、日期（DSDatePicker，必填）、开始时间 / 结束时间（默认当前整点/整点+1h）、全天开关、地点、备注；
- 校验规则同第 8.3 节；保存失败 toast 并保留表单内容；
- 编辑入口：列表项编辑按钮 / 双击列表项 / 月视图双击新建。

## 10.4 数据流

```
drift 流式查询（按月查询 + 按日查询）
  → Riverpod StreamProvider / FutureProvider
  → 月视图圆点数据 + 列表数据
  → 任何保存/删除后数据库变更自动刷新 UI（无需手动刷新）
```

## 10.5 批量删除窗口（按月勾选删除）

用户可能需要删除多个日期的事件、甚至整个月的事件，逐日逐条删除效率过低。批量删除提供"按月列出 + 勾选 + 全选"的批量选择窗口，删除结果先进回收站（可后悔），不物理删除。

- **入口**：设置 → 数据管理 → "批量删除事件"（小窗：宽 560 / 圆角 14，样式同设置弹窗）。
- **月份选择**：默认显示当前月视图所在月份；窗口顶部提供左右箭头切换月份（跨年翻月正确携带年份）。
- **事件列表**：当月所有正常事件（`deletedAt IS NULL`）按日期升序、同日按开始时间升序逐行列出；每行左侧为勾选框（DSCheckbox），行内显示：日期（如 `3月15日`）、时间区间（全天显示"全天"）、标题、地点（无地点省略）。列表可滚动（月份事件多时）。
- **全选**：列表上方"全选"勾选框：点击后当月所有事件全部勾选；再次点击全部取消；当且仅当所有事件均已勾选时全选框呈选中态。
- **已选计数**：底部左侧显示 `已选 N 个`（随勾选实时更新）。
- **删除动作**：底部"删除"按钮（未勾选任何事件时禁用）→ 弹出确认框（文案："将删除 N 个事件，删除后可在回收站恢复"）→ 确认后所选事件全部软删除（`deletedAt = now`）→ 关闭窗口 → 月视图圆点与列表经 drift 流式查询自动刷新。
- **空态**：当月无事件时显示"这个月没有事件"提示，删除按钮禁用。
- **取消**：底部"取消"按钮或点击遮罩关闭，勾选状态不保留（下次打开重新选择）。

## 10.6 回收站窗口（删除后悔余地）

所有删除（含批量删除与单条删除）先进入回收站，用户可恢复或彻底清理，防止误操作。

- **入口**：设置 → 数据管理 → "回收站"（小窗：宽 560 / 圆角 14）。
- **列表**：软删除事件（`deletedAt IS NOT NULL`）按删除时间倒序逐行列出，行内显示：原日期、时间、标题、删除时间（如 `3月12日 14:30 删除`）；行 hover 浮现"恢复"按钮。
- **恢复**：点击"恢复"→ `deletedAt = null` → 事件回到日历（月视图圆点/列表自动刷新），回收站列表即时移除该行。
- **彻底清理**：底部"彻底清理"按钮（回收站为空时禁用）→ 弹出确认框（文案："将彻底删除回收站中全部 N 个事件，此操作不可恢复"）→ 确认后物理 DELETE 回收站全部事件 → 列表清空。
- **空态**：回收站无事件时显示"回收站是空的"。

**删除一致性规则**：§10.2 今日列表的既有单条删除（hover 删除按钮 + DSDialog 确认）同步改为软删除进回收站，保证"删除即进回收站"语义全局统一。

---

# 11. 设置（弹窗）

设置以**小窗形式**呈现：左下角圆形设置按钮触发，宽 560 / 圆角 14 / 最大高 82% 视口，标题栏"设置"+ 右上角关闭按钮，可点击遮罩关闭（内容区不再重复"设置"标题）。分区：

- **外观**：界面动画总开关（日期聚焦动画、侧边栏收起、弹层过渡；默认开启，关闭以适配低性能设备）；**主题三选一：浅色 / 深色 / 毛玻璃**（DSSegmentedControl 三段，与输入条分段控件同规格；切换即时生效并持久化 `themeMode` key，重启沿用；毛玻璃规范见 §9.4.2）。
- **AI 服务（LLM 配置）**：
  - **供应商下拉选择**（预设：DeepSeek/OpenAI/通义千问/Kimi/智谱 GLM/豆包/Ollama/SiliconFlow/OpenRouter + "自定义"兜底）；选中预设自动回填 baseURL 与默认模型名（模型名仍可手动修改），自定义时显示 baseURL 输入框；
  - API Key（密文输入框，显隐切换）；
  - 模型名（预设默认值可改）；
  - "测试连接"按钮（`GET {base}/models`，成功显示绿勾"连接成功"，失败显示第 6.4 节文案）；
  - 提示行：Key 仅保存在本机。
- **数据管理**：导出 ics、导出 JSON 备份、从 JSON 备份恢复（文件选择 + 确认弹窗）、**批量删除事件**（打开 10.5 批量删除窗口）、**回收站**（打开 10.6 回收站窗口）。
- **关于**：版本号、开源许可说明、文档链接。

---

# 12. 数据导出与备份

## 12.1 ics 导出（RFC 5545）

- 全量**正常事件**导出为单个 `.ics` 文件（`file_picker` 选择保存位置）；**回收站事件（`deletedAt IS NOT NULL`）不导出**；
- 结构：

```
BEGIN:VCALENDAR
VERSION:2.0
PRODID:-//calendar//CN
BEGIN:VTIMEZONE
TZID:Asia/Shanghai
BEGIN:STANDARD
DTSTART:19700101T000000
TZOFFSETFROM:+0800
TZOFFSETTO:+0800
END:STANDARD
END:VTIMEZONE
BEGIN:VEVENT
UID:{id}@calendar.local
DTSTAMP:20250312T080000Z
DTSTART;TZID=Asia/Shanghai:20250312T090000
DTEND;TZID=Asia/Shanghai:20250312T100000
SUMMARY:...
LOCATION:...
DESCRIPTION:...
END:VEVENT
END:VCALENDAR
```

- 全天事件：`DTSTART;VALUE=DATE:20250312`（无 DTEND）；
- 时间点事件（end 为 null）：仅 DTSTART 无 DTEND；
- 文本转义：逗号→`\,`、分号→`\;`、换行→`\n`；行尾 CRLF，超 75 字节折行；
- 导出后可导入 Windows 日历 / Outlook 验证（列入 UAT，第 15.4 节）。

## 12.2 JSON 备份与恢复

```json
{
  "app": "calendar",
  "version": 1,
  "exported_at": "2025-03-12T08:00:00",
  "events": [ { "id": 1, "title": "...", "...": "同 events 表结构" } ]
}
```

- **不含 settings / API Key**（避免密钥随备份扩散）；**仅含正常事件（`deletedAt IS NULL`），回收站事件不备份**；
- 恢复校验：`app == "calendar"` 且 `version <= 1`，否则拒绝并提示；
- 合并策略：按 `id` 合并，已存在的 id 跳过（结束后提示"恢复 N 条，跳过 M 条"）；MVP 不做覆盖选项；
- 恢复只写正常事件区，**不触碰回收站内容**（恢复操作不影响已删除事件）。

## 12.3 与 git 的职责分离

- **git 只记录代码与文档**：数据库、备份、导出文件均已忽略（第 13.6 节）；
- **数据备份靠应用内导出功能**：两者互不替代；恢复数据用应用内"从备份恢复"。

---

# 13. 版本控制与迭代记录规范（用户明确要求）

## 13.1 仓库与远程策略

- 仓库位置：`calendar/` 独立本地 git 仓库（工作区根目录本身不是 git 仓库，各项目互不干扰）；
- **当前无 remote、不推送 GitHub**；保持纯本地直至用户日后决定；
- 日后推送备忘（用户同意后执行）：`git remote add origin <url>` → `git push -u origin main`；推送前检查：① `git log --all -S "sk-"` 确认历史无 API Key；② 确认 `.gitignore` 生效、无大文件；如有泄漏历史需用 filter-repo 清理后再推。

## 13.2 提交规范（Conventional Commits）

- 格式：`<type>: <中文简述>`，type ∈ `feat / fix / docs / refactor / test / chore / build`；
- 简述 ≤ 72 字符、陈述式（"实现…"、"修正…"）、单行为主；重大变更加正文说明动机；
- 示例：
  - `feat: 实现月视图日历网格与日期选择`
  - `feat: 实现本地解析器日期时间归一化规则`
  - `fix: 修正"下周X"跨月时星期计算偏移`
  - `docs: 初始技术开发文档，确定 MVP 范围与架构`
  - `chore: 升级 drift 至 2.x 并跑通全量测试`
- 单个提交聚焦单一意图；禁止把无关改动混入一个提交。

## 13.3 "重要变动"的提交时机（必须提交）

| 时机 | 提交要求 |
|---|---|
| 每个里程碑（M0–M4）完成 | 里程碑 commit + 打 tag |
| 每个功能模块完成（设计系统、月视图、LocalParser、确认流、OCR、AiParser、备份导出……） | 模块级 `feat:` commit |
| 每个 bug 修复 | `fix:` commit |
| 文档每次修订 | `docs:` commit |
| 依赖锁定变更 | `chore:` commit（附测试结果说明） |

总原则：**每次提交后工程处于可编译、可运行状态**；小步高频；禁止提交构建产物、用户数据、密钥（第 13.6 节）。

## 13.4 分支策略（单人本地）

- 默认 `main` 单分支直接开发（已由 `master` 重命名，见第 16 章）；
- 较大特性（预计 > 1 天）：开 `feature/xxx` 本地分支开发，完成后 `git merge --no-ff feature/xxx` 合回 main——**合并节点保留特性边界，形成清晰迭代脉络**，随后删除特性分支；
- 临时试验用 `experiment/xxx`，可 rebase 压缩后合并或直接废弃。

## 13.5 里程碑 tag

- 语义化版本：`v0.1.0`（M1 完成）→ `v0.2.0` → `v0.3.0` → `v1.0.0`（MVP 完成）；
- 打 tag 必须附说明：`git tag -a v0.1.0 -m "M1：设计系统+月视图+手动建事件"`。

## 13.6 .gitignore 清单（已随仓库交付，`calendar/.gitignore`）

忽略类别：Flutter/Dart 构建产物（`.dart_tool/`、`build/`、`windows/flutter/ephemeral/` 等）、IDE 文件（`.idea/`、`*.iml`、`.vscode/`）、用户数据（`*.sqlite*`、`*.ics`、`backups/`）、密钥（`.env`、`*.key`、`llm_key.txt` 等）、发布产物（`release/`、`*.msix`）。**`pubspec.lock` 不忽略**（应用类项目须提交锁定依赖）。

## 13.7 预期提交历史示例（迭代记录验收样板）

```
* v1.0.0  MVP 完成：备份导出 + 测试补全 + 打包
*   Merge branch 'feature/backup-export' (--no-ff)
|\
| * feat: 实现 JSON 备份恢复（按 id 合并去重）
| * feat: 实现 ics 导出（RFC 5545，含 VTIMEZONE 与转义）
|/
* v0.3.0  OCR 与 AI 解析接入
*   Merge branch 'feature/ocr' (--no-ff)
|\
| * feat: 接入 Windows.Media.Ocr 与图片输入途径
|/
*   Merge branch 'feature/ai-parser' (--no-ff)
|\
| * feat: 实现 OpenAI 兼容客户端与 AiParser 结构化输出
|/
* v0.2.0  输入页 + 本地解析 + 确认流
* feat: 实现确认卡片流（逐条编辑、保存入库）
* feat: 实现 LocalParser 正则与词库规则
* v0.1.0  设计系统 + 月视图 + 手动建事件
* feat: 实现 drift 事件存储与事件表单
* feat: 实现仿 macOS 设计系统组件与月视图
* chore: 初始化 Flutter Windows 工程
* docs: 初始技术开发文档，确定 MVP 范围与架构   ← 本次提交
```

---

# 14. 错误处理与边界情况（汇总表）

| 场景 | 检测 | 处理 |
|---|---|---|
| Windows 缺中文 OCR 语言包 | 引擎语言检测失败 | 回退 en 识别 + 顶部提示安装中文语言包（第 7.1 节） |
| 图片超大 | 解码后最长边 > 2000px | 等比缩放预处理（第 7.2 节） |
| 剪贴板无图片 / 文件格式不支持 | 读取为空 / 解码失败 | 提示"请粘贴图片或选择 png/jpg/bmp 文件" |
| LLM 401 / 402 / 429 / 超时 | 第 6.4 节 | 对应文案与重试策略 |
| LLM 返回非法 JSON | 本地校验失败 | 提示"AI 返回格式异常，建议切换本地模式" |
| 离线解析零命中 | 全部句子零命中 | 提示 + 引导手动新建（第 8.4 节） |
| 日期歧义（无年份、跨年、闰年） | 归一化规则表（第 5.2 节） | 就近未来原则；过去日期 UI 提示但允许保存 |
| 结束 < 开始（跨天区间） | 表单/解析校验 | 解析时 end 加一天；表单中报错提示 |
| 时间段重叠 | 查询同日事件 | MVP 允许并存，列表并列显示（不拦截） |
| DB 写入失败 | 异常捕获 | toast 报错，草稿/表单内容保留，可重试 |
| 备份文件版本不符 / 非本应用文件 | 恢复前校验 | 拒绝并提示原因（第 12.2 节） |
| 数据库迁移失败 | drift 迁移异常 | 回滚事务，提示用户导出备份并联系修复 |
| 插件源码含 UTF-8 字符（GBK 等本地代码页下 C4819 警告被 /WX 升级为错误） | 已在 `windows/CMakeLists.txt` 的 APPLY_STANDARD_SETTINGS 全局追加 `/utf-8` | 无需用户处理；任何语言环境构建稳定 |
| 窗口过窄 | 宽度 < 1000px | 右侧列表折叠为底部面板（第 9.2 节） |
| 批量删除时当月无事件 | 查询结果为空 | 空态提示"这个月没有事件"，删除按钮禁用 |
| 批量删除未勾选任何事件 | 已选计数为 0 | 删除按钮禁用 |
| 批量删除确认后部分事件已被删 | 软删除按 id 批量执行 | 幂等处理，已删项跳过，正常完成 |
| 回收站为空 | 查询结果为空 | 空态提示"回收站是空的"，彻底清理按钮禁用 |
| 恢复事件日期在过去/未来 | 无特殊约束 | 照常恢复显示（事件无唯一性约束，无冲突概念） |
| 批量删除窗口跨年翻月 | 12月↔1月切换 | 月份切换正确携带年份 |
| 彻底清理后列表未刷新 | 物理删除后 | 立即清空列表 |

---

# 15. 测试策略

## 15.1 单元测试（test/unit）

- **日期归一化用例表**（重点，≥ 20 例）：绝对日期（含/不含年份）、相对日期（今天/明天/后天/本周X/下周X/周末）、时间与时段映射、区间、跨天区间、缺省规则（无年份、无时刻）、边界（闰年 2/29、年末"3月1日"、跨月"下周X"、周日/周一星期边界）；
- **地点/事务抽取**：介词锚点、后缀词表兜底、标题生成三级规则；
- **多事件切分**：换行/标点切句、一句多动词；
- **ics 生成**：转义（逗号/分号/换行）、折行、全天 VALUE=DATE、VTIMEZONE；
- **JSON 备份**：序列化/反序列化、版本校验拒绝路径；
- **AiParser JSON 校验**：合法/缺字段/类型错误/start 无法解析等输入；
- **回收站 DAO**：软删除（deletedAt 写入）、恢复（置 null）、彻底清理（物理删除）、正常查询/ics/备份均过滤已删除事件、迁移测试（v1 → v2 新增 deletedAt 列，老数据全部 null）；
- **主题 token**：`themeMode` 持久化读写；`DSTokens.dark` 分层亮度递进（主背景 < 卡片 < 弹层）且无投影依赖；`DSTokens.glass` 与 `light` 派生关系（文字/强调色一致，仅背景/描边/模糊不同）。

## 15.2 Widget 测试（test/widget）

- 确认卡片：字段编辑联动、缺失字段红色高亮、保存按钮禁用逻辑、已保存态；
- 日历月视图：今日高亮、选中态、事件圆点渲染、翻页边界（一月/十二月）；
- 事件表单：标题空校验、结束 < 开始校验、全天开关隐藏时间控件；
- 设置页：模式切换持久化、测试连接成功/失败提示；
- 批量删除窗口：月份切换与事件行渲染、逐条勾选/取消、全选/取消全选、已选计数、未勾选时删除禁用、删除确认后进回收站、空态；
- 回收站窗口：列表渲染（删除时间倒序）、恢复后行消失且日历可见、彻底清理确认与清空、空态；
- 设置页主题：三段主题控件切换 → token 即时切换、重启后沿用；动画关闭时主题切换瞬时完成。

## 15.3 集成测试（test/integration）

- `AiParser` 注入 `FakeOpenAiClient`（不发真实请求）：验证请求参数组装、成功路径解析、401/429/超时/非法 JSON 各分支；
- drift 迁移：v1 数据库升级路径与数据保留；
- 批量删除全流程：勾选 → 删除确认 → 事件从月视图/列表消失 → 回收站可见 → 恢复后回到日历 → 彻底清理后消失（真窗口）；
- 主题渲染：深色主题下卡片无投影、层次分明（亮度分层 + hairline）；毛玻璃主题下主界面/弹层正常渲染不卡顿（真窗口）。

## 15.4 手动 UAT 清单（发布前逐条过）

1. 粘贴"3月15日下午2点去上海虹桥机场出差"→ 本地解析 → 卡片字段正确 → 保存 → 月视图 3 月 15 日出现蓝点、列表可见；
2. 输入"明天上午10点在公司3楼会议室开会，讨论季度计划"→ 回车发送 → 结果面板标题/地点/备注正确；Shift+Enter 可换行；
3. 截图一张含会议通知的图片：① 经输入条"上传"按钮选择 → 附件缩略图出现 → 点发送 → OCR+解析 → 结果面板；② 将图片拖拽到窗口任意位置 → 同样进入附件；③ 附件右上角"×"可删除；
4. 切换 AI 模式（填 Key）解析同一文本 → 结果进入同一确认流；模式选择在输入条右侧分段控件，重启后沿用；
5. 故意清空 API Key → AI 解析报"检查 Key"文案，本地模式不受影响；
6. 断网状态下：本地模式正常解析，AI 模式报网络错误并建议切本地；
7. 确认卡片删除一条 → 不写库；全部保存 → 日历刷新可见；
8. 缺失字段（"记得交房租"）→ 卡片红色提示补时间 → 补全后保存成功；
9. 点击今日列表"＋"按钮新建全天事件 → 列表"全天"分区显示；
10. 导出 ics → 导入 Windows 日历/Outlook 显示正确（含全天事件）；
11. JSON 备份 → 清空应用数据 → 恢复 → 事件完整、API Key 未被备份；
12. 每次里程碑后检查 git 提交与 tag 完整（第 13.7 节样板对照）。

---

# 16. 开发里程碑与打包发布

> 里程碑 M0–M4 为后续实施排期；每个里程碑独立 commit + tag（第 13 章）。

## 16.1 里程碑定义

| 里程碑 | 内容 | 完成判据 | tag |
|---|---|---|---|
| M0 工具链 | 安装 Flutter SDK（第 2.4 节）、`flutter create` 空工程跑通 | `flutter run -d windows` 出窗口 | —（commit 即可） |
| M1 基础框架 | 设计系统组件 + 无边框窗口 + 月视图 + 今日列表 + 事件表单 + drift 存储 + 手动增删改 | UAT 9 通过；浅深色主题正常 | v0.1.0 |
| M2 本地解析 | 输入页 + LocalParser + 确认卡片流 | UAT 1/2/7/8 通过 | v0.2.0 |
| M3 图片与 AI | OCR（含语言包回退）+ AiParser + 设置页（LLM 配置/测试连接） | UAT 3/4/5/6 通过 | v0.3.0 |
| M4 收尾 | ics/JSON 导出恢复 + 测试补全 + 打包 | UAT 10/11 通过；全量测试绿 | v1.0.0 |

## 16.2 打包发布

- Release 构建：`flutter build windows --release`，产物 `build/windows/x64/runner/Release/`（exe + dll + data 目录，整体目录分发）；
- 可选 MSIX：用 `msix` 打包工具产出安装包；签名先用自签名证书测试安装，正式分发再配置代码签名证书；
- 版本号：pubspec `version: X.Y.Z+build`，与 git tag 语义化版本一致；窗口标题与应用显示名统一为 `calendar`。

## 16.3 仓库初始化动作（已在本次执行）

- `calendar/` 内 `git init`（分支 `master` → 重命名 `main`）；
- 交付 `.gitignore` 与本文档，以规范提交收尾（`docs: 初始技术开发文档，确定 MVP 范围与架构`）。

---

# 17. 风险与开放问题

| # | 风险 / 开放问题 | 应对 |
|---|---|---|
| 1 | OCR 引擎生态风险（早期：windows_ocr 停更、winrt_ocr_flutter 依赖 WinRT 且需 MSIX 包身份） | 已切换 **RapidOCR（ONNX + PP-OCRv5）**：跨平台一套模型、无需包身份；OcrService 接口隔离，仍可换平台原生引擎（7.3 节） |
| 2 | 本机尚未安装 Flutter SDK | M0 安装步骤已写入（第 2.4 节） |
| 3 | DeepSeek API 稳定性与计费 | AI 模式可随时切回本地模式；provider 抽象便于换服务商 |
| 4 | 仿 macOS 视觉的版权边界 | 仅自绘"风格"；不打包 SF 字体、苹果图标等版权资产（第 9.3 节） |
| 5 | 离线解析对口语化/中文数字日期覆盖有限 | 文档明示限制（第 5.2 节），此类文本引导 AI 模式 |
| 6 | API Key 明文存储（MVP 接受） | 二期 DPAPI 加密（第 4.5 节）；备份不含 Key |
| 7 | 移动端二期平移成本 | 解析器/存储/领域层与 UI 解耦（第 3 章），仅重写 UI 层 |
| 8 | 将来推送 GitHub 时的敏感信息泄漏 | 推送前检查清单已写入（第 13.1 节） |

---

# 附录 A：离线解析词表（初始清单，随迭代扩充）

**地点后缀词表**：市、省、区、县、镇、乡、村、路、街、道、巷、大厦、大楼、楼、广场、中心、酒店、饭店、机场、火车站、高铁站、地铁站、车站、码头、港口、园区、科技园、工业园、学校、大学、学院、医院、银行、公司、集团、单位、总部、分部、门店、店、会议室、办公室、体育馆、图书馆、公园、景区。

**地点介词锚点**：去、到、在、前往、飞往、抵达、出差至、出发去、住在。

**事务关键词表**：出差、开会、会议、例会、面试、考试、测验、体检、聚会、聚餐、拜访、约见、见面、签约、培训、上课、讲座、报告、答辩、值班、搬家、婚礼、生日、纪念日、约会、团建、旅游、出发、回国、返程、注册、报名、提交、交、截止、还款、缴费、续费、取件、快递、就诊、复诊、复查。

（词表为独立常量文件，新增词条以 `feat:` commit 记录迭代。）

# 附录 B：核心正则示例（Dart，集中于 `core/regex_patterns.dart`）

```dart
/// 绝对日期：2025年3月12日 / 2025-03-12 / 3月12日 / 3-12
final RegExp absoluteDate = RegExp(
  r'(?:(?<year>\d{4})\s*[年/-])?\s*(?<month>\d{1,2})\s*[月/-]\s*(?<day>\d{1,2})\s*日?',
);

/// 相对日期词
final RegExp relativeDateWord =
    RegExp(r'(今天|明天|后天|大后天|昨天|前天|本周[一二三四五六日天]|下周[一二三四五六日天]|周[一二三四五六日天]|周末)');

/// 时间：下午3点半 / 15:30 / 晚上8点20分
final RegExp timeExpr = RegExp(
  r'(?<slot>凌晨|早上|上午|中午|下午|傍晚|晚上)?\s*'
  r'(?<hour>\d{1,2})\s*[点时:：]\s*(?<minute>半|\d{1,2}\s*分?)?',
);

/// 区间：X点到Y点
final RegExp timeRange = RegExp(
  r'(?<startHour>\d{1,2})\s*[点时:：]\s*(?<startMinute>半|\d{1,2}\s*分?)?\s*[到至\-~]\s*'
  r'(?<endHour>\d{1,2})\s*[点时:：]\s*(?<endMinute>半|\d{1,2}\s*分?)?',
);
```

（完整规则、时段映射与缺省算法见第 5.2 节；所有正则只允许在 `regex_patterns.dart` 中定义，禁止散落各处。）

# 附录 C：参考链接

- `flutter_onnx_ocr` 包（RapidOCR：ONNX Runtime + PP-OCRv5，FFI 跨平台）：https://pub.dev/packages/flutter_onnx_ocr
- `onnxruntime_v2` 包（全平台推理后端）：https://pub.dev/packages/onnxruntime_v2
- RapidOCR 官方仓库（模型来源）：https://github.com/RapidAI/RapidOCR
- DeepSeek API 文档 · JSON Output：https://api-docs.deepseek.com/guides/json_mode/
- DeepSeek API 文档（OpenAI 兼容）：https://api-docs.deepseek.com/
- Flutter Windows 桌面文档：https://docs.flutter.dev/platform-integration/windows/building
- drift 文档：https://drift.simonbinder.eu/
- Riverpod 文档：https://riverpod.dev/
- Windows.Media.Ocr 官方文档：https://learn.microsoft.com/windows/uwp/develop/ocr
- RFC 5545（iCalendar）：https://datatracker.ietf.org/doc/html/rfc5545
