# Calendar 日历应用 · 技术开发文档

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.61 |
| 产品定位 | Windows 桌面端轻量事务日历 |
| 文档用途 | 当前实现说明与后续维护约定 |
| 最后更新 | 2026-09-16 |

本文按当前代码整理。保留原章节编号，便于对照代码注释；修订记录描述历史版本，当前行为以正文与代码为准。

维护原则：围绕“输入、解析、确认、日历”做好单一流程。优先保持轻量、易用和可维护；架构拆分、依赖升级与功能扩展按实际需求推进。

快速定位：功能见第 1 章，代码入口见第 3 章，数据规则见第 4/12 章，界面见第 9–11 章，开发与发布见第 13/15/16 章。

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-08-29 | 初始版本：确定 MVP 范围、技术选型、架构、数据模型、解析引擎、UI 规范、版本控制规范与里程碑 |
| v0.2 | 2026-08-29 | 修正文档日期；按文档进入开发阶段（M0 起，里程碑推进中） |
| v0.3 | 2026-08-29 | 开发落实：OCR 方案定为 winrt_ocr_flutter（windows_ocr 停更核实）；记录 /utf-8 构建修复 |
| v0.4 | 2026-08-29 | 开发推进：M0–M3 完成（工程/设计系统/日历/本地与 AI 解析/OCR/设置页），tag v0.1.0–v0.3.0；修订记录随里程碑更新 |
| v0.5 | 2026-08-29 | MVP 完成：M4 交付（ics 导出/JSON 备份恢复/测试补全/Release 打包），tag v1.0.0；后续进入手动 UAT 与二期 backlog |
| v0.6 | 2026-08-29 | UI 调整：窗口按钮改为 Windows 风格右上角（移除交通灯）；AI 服务改为供应商下拉选择（预设+自定义）；tag v1.0.1 |
| v0.7 | 2026-08-29 | 修复灰屏 bug：DSTokensScope 上移至 MaterialApp.builder（弹层此前无法访问主题，Release 空指针）；补充透明 Material 祖先；新增集成回归测试（快速点击日期/双击表单）；tag v1.0.2 |
| v0.8 | 2026-08-29 | 字体规范落地：显式指定 Segoe UI Variable→微软雅黑回退链；字重统一为 400/700（消除合成加粗）；tag v1.0.3 |
| v0.9 | 2026-08-29 | 字体方案定稿：打包 HarmonyOS Sans SC 作为中英文唯一主字体（含许可合规声明）；tag v1.0.4 |
| v0.10 | 2026-08-29 | 解析规则调整：无年份默认当前系统年（取消"已过取明年"偏移）；窗口启用 Win11 DWM 原生圆角；tag v1.0.5 |
| v0.11 | 2026-08-29 | 交互即时性修复：按钮区与双击手势区分离、日期格移除双击新建（单击不再延迟 300ms），新建入口改为今日列表"＋"按钮；tag v1.0.6 |
| v0.12 | 2026-08-29 | 输入体验重构：移除侧边栏"新建"与整页输入页，改为日历主界面底部 AI 对话式圆角输入条（上传/附件/拖拽/图文并存/回车发送/模式分段），解析结果以独立面板呈现；设置页移除解析模式切换；圆角/hover 风格全面统一；tag v1.1.0 |
| v0.13 | 2026-08-29 | 侧边栏支持边缘把手点击收起/展开（200ms 动画，状态持久化）；tag v1.1.1 |
| v0.14 | 2026-08-29 | 侧边栏把手改为悬停浮现式：默认隐藏，鼠标靠近右边界线时淡入、离开淡出；tag v1.1.2 |
| v0.15 | 2026-08-29 | 收起把手优化：改为 overlay 覆盖交界线（去除 40px 占位空白），按钮缩小为 16×32 贴合分界线；tag v1.1.3 |
| v0.16 | 2026-08-29 | 日期选中聚焦动画：首次从今天弹动/原地浮现，后续丝滑移动（380ms，easeOutBack/easeInOutCubic）；tag v1.1.4 |
| v0.17 | 2026-08-29 | 输入条支持 Ctrl+V 粘贴：剪贴板为图片时作为附件（临时文件随删除清理），为文本时正常粘贴；日期格加防溢出保护；tag v1.1.5 |
| v0.18 | 2026-08-29 | OCR 引擎切换为 RapidOCR（ONNX + PP-OCRv5，跨平台一套模型，无需 MSIX 包身份）；新增 OCR 集成测试（合成中文图片离线识别）；tag v1.2.0 |
| v0.19 | 2026-08-29 | AI 解析年份缺省规则：无年份默认当前系统年（prompt 注入当前年 + 客户端兜底强制/宽松解析），与本地模式对齐；tag v1.2.1 |
| v0.20 | 2026-08-29 | "仅提供日"规则（双模式）：只给"日"时年月取系统当前（15号=本月15日），含当月天数校验；tag v1.2.2 |
| v0.21 | 2026-08-29 | OCR 后处理：阅读顺序排序 + 同行/折行合并（恢复被排版换行截断的完整句）；解析层标题行过滤与关键词排除后缀（通知类图片从 4 个垃圾事件 → 1 个正确事件）；tag v1.2.3 |
| v0.22 | 2026-08-29 | 聚焦动画重构：全程单圆（动画期间隐藏静态选中圆）、Apple 风格缓动曲线、跨月点击在新网格播放动画；设置页新增动画总开关（日期聚焦/侧边栏/弹层过渡）；tag v1.2.4 |
| v0.23 | 2026-08-29 | 修复聚焦动画末段回弹晃动：位置曲线改为无 overshoot（easeOutCubic），弹动感转移至缩放弹性；tag v1.2.5 |
| v0.24 | 2026-08-29 | 修复动画终点"瞬间上移"跳变：动画终点对齐静态圆实际中心（格子中心 −3.5px，与 _DayCell 列内容居中一致）；tag v1.2.6 |
| v0.25 | 2026-08-29 | 修复快速连续点击动画消失：whenCompleteOrCancel 在 reset 取消时误隐藏动画圆，改用 whenComplete；快速点击时从当前插值位置续接动画；tag v1.2.7 |
| v0.26 | 2026-08-29 | 界面重构：删除侧边栏与收起把手（日历占满全屏）；设置改为左下角圆形按钮 + 弹窗小窗；输入条底部居中减短（maxWidth 640）；tag v1.3.0 |
| v0.27 | 2026-08-29 | 解析模式分段控件滑块化：本地/AI 切换时选中块白底阴影**平滑滑动**（easeInOutCubicEmphasized，150ms），受设置页动画总开关控制（关闭时瞬间就位）；控件改为**内容自适应**布局（TextPainter 测量段宽），可在 Row 非 flex 槽位使用（Flex 对非 flex 子元素主轴不限制宽度，不得依赖父约束）；tag v1.3.1 |
| v0.28 | 2026-08-29 | 设计补充：**批量删除事件**（设置→数据管理入口，按月列出事件+逐条勾选+全选+已选计数，删除前确认，删除进回收站）与**回收站**（软删除 `deletedAt` 字段、恢复、彻底清理不可恢复警告；单条删除同步改为进回收站；ics/JSON 备份仅含正常事件）；文档先行，代码实现随后，tag 待实现后打 |
| v0.29 | 2026-08-29 | 实现 v0.28 设计：events 表新增 `deletedAt`（schemaVersion 2 + 迁移）；EventDao 软删除/批量软删除/恢复/彻底清理/回收站查询，正常查询与导出统一过滤；新增 DSCheckbox、批量删除窗口（§10.5）、回收站窗口（§10.6）；设置页数据管理入口；单条删除/撤销保存改文案；测试：DAO 6 项、迁移 1 项、Widget 3 项、集成 1 项；tag v1.3.2 |
| v0.30 | 2026-08-29 | 移除回收站入口数量徽标（按钮右侧不再显示待清理数量；删除 trashCountProvider/trashEventsProvider 与对应测试断言）；tag v1.3.3 |
| v0.31 | 2026-08-29 | 月视图顶栏"今天"按钮文案改为"跳回今天"（语义更明确）；同步更新 §10.1 与测试断言；tag v1.3.4 |
| v0.32 | 2026-08-29 | 设置弹窗去重：内容区移除重复的"设置"大标题（标题栏已表达），分区直接承接；tag v1.3.5 |
| v0.33 | 2026-08-29 | 星期表头样式增强：字号 11→12、颜色加深（textSecondary→textPrimary 加粗 w600）、表头下新增分隔线，强化与日期区的层次分界；tag v1.3.6 |
| v0.34 | 2026-08-29 | 月视图年月标题可点击：hover 浮现淡色圆角底色提示；点击弹出 DSYearPicker 年份选择器（12 年一页网格 + 翻页），选中后保持月份跨年跳转；新增对应 widget 测试；tag v1.3.7 |
| v0.35 | 2026-08-29 | 年月标题 hover 提示去除边框线（仅保留淡色圆角底色）；tag v1.3.8 |
| v0.36 | 2026-08-29 | 设计补充：设置主题三选一（浅色/深色/毛玻璃）；深色按 Apple HIG Dark Mode 规范（不纯黑、亮度分层代替阴影、强调色提亮、对比度达标）；毛玻璃按 Apple HIG Materials 规范（blur 20–24 + 饱和度补偿 1.8 + 半透明白着色 + hairline 描边，层次递进）；`themeMode` 设置项与 token 扩展设计；文档先行，代码实现随后，tag 待实现后打 |
| v0.37 | 2026-08-29 | 实现 v0.36 设计：`themeMode` 设置项（默认浅色，不再跟随系统）；`DSTokens` 新增 glass 主题与 dark 弹层色 `#3A3A3C`、投影色 token（深色移除投影）；新增 `DSGlassSurface`（BackdropFilter blur 20 + ColorFilter 饱和度 1.8 + 半透明白 + hairline，浅色/深色退化为普通容器）；设置页外观新增主题三段控件（即时生效并持久化）；弹层/输入条/卡片阴影与背景统一走 token；测试：设置 DAO 2 项、token 1 项、Widget 1 项、集成主题切换；tag v1.3.9 |
| v0.38 | 2026-08-29 | 毛玻璃质感增强：着色改为**反光渐变**（左上白 0.4 → 右下透明 0.1）替代纯色高不透明度；新增**渐变边缘描边**（CustomPaint 1.5px，白 0.5 → 0.12）模拟玻璃边缘反射；阴影改 `spreadRadius: -1` 贴合边缘；输入条接入 DSGlassSurface；主背景玻璃渐变层次；参数依据 devgex/掘金毛玻璃教程；tag v1.4.0 |
| v0.39 | 2026-08-29 | 毛玻璃覆盖范围补全：主界面改"装饰光斑背景 + 全屏 BackdropFilter 模糊层 + 清晰内容层"三层结构（整窗玻璃质感）；设置分区卡片（_SectionCard）接入 DSGlassSurface（嵌套玻璃）；DSGlassSurface 支持非玻璃分支 border/无阴影开关；tag v1.4.1 |
| v0.40 | 2026-08-29 | 修复主界面无透明感：内容层与标题栏背景改为透明（原半透明白盖死模糊层）；模糊层白度降至 0.35→0.18、光斑饱和度提升，光斑经模糊清晰透出；tag v1.4.2 |
| v0.41 | 2026-08-29 | 回归 Apple HIG 标准：**移除装饰光斑与整窗模糊**（用户明确不要假背景）；窗口主背景改实色（极浅白渐变），毛玻璃仅用于浮层（输入条/弹窗/分区卡片模糊背后真实内容）；tag v1.4.3 |
| v0.42 | 2026-08-29 | 修正毛玻璃主题刺眼问题：① **撤销全局强调色改动**（主界面 accentBlue 等保持不变）；② 根因修复——移除 DSGlassSurface 的 ColorFiltered 饱和度补偿（它会作用于容器内所有前景内容，把开关/按钮颜色重新拉饱和）；③ 新增 `softAccentBlue`/`softSuccessGreen` 柔和色 token（浅色/深色与主色一致），仅设置窗口动画开关与保存按钮在毛玻璃主题下使用；tag v1.4.6 |
| v0.43 | 2026-08-29 | 柔和色微调：`softAccentBlue` `#4D8FE0` / `softSuccessGreen` `#45B97C`（比主色低一档饱和度、保持鲜活不发灰，去掉半透明）；tag v1.4.7 |
| v0.44 | 2026-08-29 | 修复分区卡片"左白右黑"：反光渐变右端过透明（白 0.1）露出背后暗色 → 两端均保持白底（0.32→0.2）；分区卡片改 `blur: false`（弹窗已模糊，卡片仅着色+描边，避免嵌套 BackdropFilter 采样异常）；DSGlassSurface 新增 blur 开关；tag v1.4.8 |
| v0.45 | 2026-08-29 | 修复毛玻璃弹窗随遮罩变暗：dialogBackground 不透明度 0.85 → 0.94（弹窗叠在 0.32 遮罩上保持明亮，Apple 浮层设计）；tag v1.4.9 |
| v0.46 | 2026-08-29 | 修复无边框窗口无法拖拽缩放：window_manager setAsFrameless 移除系统缩放热区（四边 NCHITTEST 均返回 HTCLIENT）；在 `windows/runner/win32_window.cpp` 手动处理 WM_NCHITTEST（边缘 6px 返回 HTLEFT/HTRIGHT/HTTOP/HTBOTTOM 及四角），实测命中正确；tag v1.5.0 |
| v0.47 | 2026-08-29 | 缩放方案修正（v1.5.0 光标不显示）：C++ NCHITTEST 热区在 Flutter 引擎光标系统下不显示缩放光标；改用 window_manager 官方方案——Flutter 侧 `_WindowResizeEdges`（四边/四角 6px MouseRegion 显示 SystemMouseCursors 缩放光标 + 按下调 startResizing 进入系统缩放循环）；还原 C++ 改动；tag v1.5.1 |
| v0.48 | 2026-08-29 | 缩放功能修正（v1.5.1 拖动无效）：系统 SC_SIZE 缩放循环期间 WM_MOUSEMOVE 被 Flutter 引擎 HandleTopLevelWindowProc 拦截，DefWindowProc 收不到移动事件；改为**手势驱动缩放**——四边/四角 6px MouseRegion（光标）+ GestureDetector pan（按下记初始 bounds、拖动按边缘方向计算新位置/尺寸、setBounds 实时应用）；还原 C++ 试验改动；tag v1.5.2 |
| v0.49 | 2026-08-30 | 修复手势缩放闪烁跳动：根因是将 `DragUpdateDetails.delta` 单帧增量反复应用到初始 bounds，且多个异步 `setBounds` 请求并发乱序覆盖；改为按下当帧记录起点、`screen_retriever` 读取系统绝对光标并计算累计位移，串行合并窗口更新，几何层约束 900×600 最小尺寸；新增 5 项几何回归测试并完成真实窗口四边拖动验证；tag v1.5.3 |
| v0.50 | 2026-08-31 | 重构毛玻璃主题为“冷静通透”视觉体系：主背景改为 `#F3F6FA` + 淡蓝灰环境光晕，统一低饱和蓝绿强调色；`DSGlassSurfaceKind` 区分轻玻璃浮层（白 72% / blur 16）与强玻璃弹窗（白 85% / blur 20），基础填充真正参与渲染；模态遮罩降为 16% 冷灰；移除渐变边缘画笔、重复 softAccent token 与设置分组嵌套玻璃，下拉菜单接入轻玻璃；更新主题/Widget 测试并完成 106 项全量测试、Release 构建、1100×720 与 903×613 真实窗口验证 |
| v0.51 | 2026-08-31 | 按 Apple Liquid Glass 官方设计原则重构动态材质：`DSGlassSurface` 使用 `ImageFilter.compose` 组合背景模糊与轻微放大折射，增加多层透镜边缘、轻微色散、指针跟随镜面高光及按压形变；透明度调整为 clear 白 24% / blur 7.5、regular 白 56% / blur 14，弹窗统一 22px 同心圆角；环境背景增强蓝、紫、薄荷色反射，设置按钮、发送按钮与分段控件接入交互玻璃；完成 `flutter analyze`、106 项全量测试、Windows Release 构建及 1100×720→903×613 自由缩放验证 |
| v0.52 | 2026-08-31 | 依据实物透镜参考图重塑 Liquid Glass 边缘：删除默认 0.8px 硬描边与高饱和彩色细环，改为按短边自适应的 4–13px 宽弧面透镜带（柔化环境反射 + 低饱和环向反射 + 模糊内缘焦散 + 0.75px 中性外高光）；小型圆形控件使用紧凑景深，设置按钮 hover 改为中性白反光与深色图标；完成 `flutter analyze`、106 项全量测试、Windows Release 构建及唯一新进程实机检查 |
| v0.53 | 2026-08-31 | 发送按钮由双圆环改为单层玻璃圆环：`DSGlassSurface` 新增 `singleLayerEdge`，开启后保留蓝色着色主体、连续宽弧面反光与 0.75px 外轮廓，但跳过独立内缘焦散环；当前仅发送按钮启用，其他玻璃浮层保持完整层次；新增 Widget 接口断言，完成静态分析、106 项全量测试、Windows Release 构建和实机检查 |
| v0.54 | 2026-08-31 | 修正 v0.53 仍残留外圈的问题：发送按钮完全脱离 `DSGlassSurface`，删除临时 `singleLayerEdge` 接口，改为独立 32px 单一蓝色圆盘；径向亮暗渐变、hover 与按压变化全部裁剪在同一圆面内，仅保留非轮廓型柔和投影，不再生成玻璃宽边缘或外高光；Widget 测试验证发送按钮不是玻璃容器，完成静态分析、106 项全量测试、Windows Release 构建及唯一进程实机检查 |
| v0.55 | 2026-08-31 | 增强 Liquid Glass 对下层界面的透色与模糊：clear 基础白降至 14%、blur 提至 10.5，regular 基础白降至 32%、blur 维持 14；环境可读性白蒙层由约 15% 降至约 9%，蓝紫/薄荷径向光更鲜明，使背景色经模糊后真实进入玻璃；发送圆盘在毛玻璃主题改为 34% 蓝色着色（hover 42%）配深色图标，由父输入条统一采样背景且不增加嵌套 `BackdropFilter`，浅色/深色仍保持实心蓝；完成 `flutter analyze`、106 项全量测试、Windows Release 构建与主界面/设置弹窗实机检查 |
| v0.56 | 2026-08-31 | 精简设置页信息密度：删除主题的 Liquid Glass 说明、数据管理的导出/恢复/回收站说明及“关于”中的 HarmonyOS Sans 说明，同时移除对应间隔组件；主题控件直接承接分组标题，数据管理在第二排按钮处结束，“关于”仅保留版本信息，功能行为与随包字体 LICENSE 均不变；新增 Widget 回归测试，完成 `flutter analyze`、107 项全量测试、Windows Release 构建及设置页上下分区实机检查 |
| v0.57 | 2026-08-31 | 将毛玻璃设置窗口重构为单块连续玻璃：内部五个设置分组取消半透明底色、完整描边、圆角和分组阴影，直接以标题、留白及控件自身边界排版在唯一的 regular glass 弹窗上；玻璃模式分组水平内缩 4px、垂直内缩 8px，分组净间隔约 22px；浅色/深色仍保留原卡片回退。Widget 测试验证主题切换时分组根节点由 `Container` 变为透明 `Padding`，完成 `flutter analyze`、107 项全量测试、Windows Release 构建及设置页上下区域实机检查 |
| v0.58 | 2026-08-31 | 修复强玻璃弹窗仍呈现双重轮廓：根因是外侧 0.75px 高光之外，又在向内 7–13px 的 `innerRRect` 绘制独立焦散线，环形透镜带的内边界进一步强化了“玻璃套玻璃”。`dialog` 改为单轮廓材质，删除独立内缘焦散与硬边环带，使用 8px 宽、blur 4.2 的羽化反射从唯一外轮廓向内连续衰减；`floating` 小控件保留集中弧面反馈。完成 `flutter analyze`、107 项全量测试、Windows Release 构建及设置弹窗实机对照检查 |
| v0.59 | 2026-08-31 | 优化毛玻璃设置窗口渐入渐出流畅度：原150ms `FadeTransition + ScaleTransition(0.97→1)` 会使560px `BackdropFilter`、折射矩阵与边缘画笔随几何缩放逐帧重采样，约9帧内易出现跳帧。毛玻璃路径改为固定尺寸的220ms纯透明度过渡，进入使用 `easeOutCubic`、退出使用 `easeInCubic`；浅色/深色仍保留150ms淡入缩放，动画总开关关闭时两者均瞬时完成。Widget 测试断言玻璃路径不存在 `ScaleTransition`，完成 `flutter analyze`、107项全量测试、Windows Release构建及打开/关闭过程实机采样 |
| v0.60 | 2026-09-16 | 将技术文档整理为轻量项目维护手册，保留历史修订与章节编号；同步当前功能、代码组织、备份恢复规则、窗口与主题行为、版本及验证发布约定；删除过时初始化教程和重复规格，不修改程序实现 |
| v0.61 | 2026-09-16 | 本机部署 Qwen3.5-4B Q4_K_M，Ollama 推理核心升级至 0.34.0，以 Vulkan 使用 RTX 4060；记录本地调用配置与文字／截图样例验证，程序尚未接入图片直传 |

---

# 1. 项目概述与范围

## 1.1 产品目标

用户输入含时间、地点、事项的文本或图片，程序整理为可编辑的事件草稿。经用户确认保存后，事件写入本机日历。

## 1.2 核心用户流程

文本／图片附件 → 图片经本地 OCR 转为文字 → 合并原文 → 本地或 AI 解析 → 待确认事件 → 保存 → SQLite → 日历刷新。

解析失败保留输入，成功后清空输入并打开结果面板。草稿仅在内存中，关闭未保存结果需要确认。

## 1.3 已实现功能

- 月视图、翻月、年份选择、跳回今天、选中日期事件列表。
- 从列表“＋”新建事件，以及编辑、单条删除、按月批量删除。
- 文本输入、多图上传、全窗口拖入图片、Ctrl+V 粘贴；Enter 发送，Shift+Enter 换行。
- 本地规则解析与 OpenAI 兼容 AI 解析，共用草稿确认、编辑、逐条保存与全部保存。
- 回收站恢复与彻底清理、ICS 导出、JSON 备份恢复。
- 浅色、深色、毛玻璃主题，主题与动画偏好持久化。
- 无边框窗口、自绘标题栏与四边／四角自由缩放。

## 1.4 范围边界

当前专注 Windows 单机使用。多端同步、移动端、重复事件、通知提醒、周／日视图等尚未实现，也不是必须执行的开发排期。

MiniCPM 等端侧语言模型仅做过选型讨论；目前没有模型下载管理、内置 LLM 推理引擎或专用配置入口。已有 Ollama 服务预设可连接用户自行部署的模型。

## 1.5 系统要求

面向 Windows 10 21H2+／Windows 11 x64。Release 目录包含 Flutter 运行时与 OCR 资源；使用本地解析无需联网，AI 模式需要能够访问用户配置的服务。模型运行性能由对应本地或远程服务决定。

## 1.6 术语

- 事件：已确认并写入数据库的记录。
- 草稿：解析生成、尚未保存的 `ParsedEvent`。
- 本地模式：Dart 正则与词表解析，不运行语言模型。
- AI 模式：调用配置的 LLM 服务；服务可以位于本机。
- 回收站：同一事件表中标记为软删除的记录。

# 2. 技术选型与开发环境

## 2.1 框架

Flutter + Dart，目标为 Windows 桌面。MaterialApp 提供导航、主题等基础宿主能力，业务视觉由自建 DS 组件统一。

## 2.2 主要依赖

| 依赖 | 用途 |
|---|---|
| flutter_riverpod | 状态与依赖管理 |
| drift / SQLite | 本地事件和设置存储 |
| dio | LLM HTTP 请求 |
| flutter_onnx_ocr | RapidOCR / ONNX Runtime / PP-OCRv5 |
| window_manager / screen_retriever | 窗口管理、绝对光标位置 |
| desktop_drop / file_picker / pasteboard | 拖拽、文件选择、剪贴板 |
| path_provider / path / intl | 文件位置、路径、日期格式化 |
| flutter_test / integration_test | 单元、Widget 与桌面集成测试 |
| build_runner / drift_dev | 数据库代码生成 |

版本范围见 `pubspec.yaml`，实际锁定版本见 `pubspec.lock`。提交锁文件；仅在功能或兼容性需要时升级依赖。

## 2.3 轻量化约定

复用现有解析器、存储和 DS 组件。不要为目录形式或抽象层数单独重构；出现重复逻辑或明确测试需求时，再提取小型服务。新增依赖应有直接用途。

## 2.4 开发环境

项目已完成初始化。开发需要 Flutter SDK、Git，以及包含“使用 C++ 的桌面开发”工作负载的 Visual Studio。使用 `flutter doctor -v` 检查当前机器，不依赖历史安装路径或旧环境状态。

# 3. 代码结构与业务入口

## 3.1 当前组织方式

代码按界面、数据服务、领域模型和设计系统组织。Riverpod 管理数据库依赖和事件流；部分界面状态类直接编排 DAO、OCR、LLM 调用，这是当前小型项目的实现方式，没有独立的 application 目录。

## 3.2 关键文件

| 路径 | 职责 |
|---|---|
| `lib/main.dart` | 初始化桌面窗口并启动 ProviderScope |
| `lib/app/app.dart` | MaterialApp、主题选择、DSTokensScope |
| `lib/features/shell/app_shell.dart` | 标题栏、背景、设置入口、拖图、窗口缩放 |
| `lib/features/shell/window_resize_geometry.dart` | 窗口缩放几何计算 |
| `lib/features/calendar/` | 月历、日期列表、批量删除、回收站 |
| `lib/features/event/event_form.dart` | 手动新建与编辑 |
| `lib/features/intake/chat_composer_bar.dart` | 输入、图片附件、OCR 和解析编排 |
| `lib/features/intake/parse_result_panel.dart` | 草稿列表与批量保存 |
| `lib/features/intake/confirm_card.dart` | 单条草稿编辑、校验、保存与撤销 |
| `lib/features/settings/settings_page.dart` | 外观、AI 配置、导入导出 |
| `lib/data/parsers/` | EventParser、LocalParser、AiParser |
| `lib/data/llm/openai_compatible_client.dart` | HTTP 协议、错误转换与 JSON 解码 |
| `lib/data/ocr/ocr_service.dart` | OCR 初始化、图片缩放、识别后处理 |
| `lib/data/db/` | 表、迁移、DAO、Riverpod providers |
| `lib/data/export/` | ICS 与 JSON 备份恢复 |
| `lib/domain/` | 草稿、来源类型、词表和供应商预设 |
| `lib/core/` | 常量、解析正则、错误类型 |
| `lib/shared/design/` | 主题 Token、玻璃材质与通用 DS 控件 |

## 3.3 解析器接口

`EventParser.parse(String text)` 返回 `Future<List<ParsedEvent>>`。本地与 AI 解析输出相同草稿类型，界面复用确认流程。AiParser 依赖 `LlmGateway`，测试可注入 fake；解析器可注入时钟以固定日期用例。

## 3.4 维护边界

日期与信息抽取放在解析器中，数据库查询放在 DAO 中，材质和通用视觉放在设计系统中。界面负责交互和流程编排。优先在现有职责范围内修改，避免无实际收益的分层扩展。

# 4. 数据模型与存储

## 4.1 数据库

当前 `schemaVersion = 2`，包含两张表：

| 表 | 字段与用途 |
|---|---|
| events | id、title、location、start、end、allDay、note、sourceType、sourceText、createdAt、updatedAt、deletedAt |
| settings | key/value 字符串键值设置 |

`deletedAt == null` 为正常事件；删除设置时间戳，恢复清空时间戳，彻底清理物理删除。普通日／月查询和导出均排除回收站记录。

主要设置：`parseMode`（默认 local）、`llmBaseUrl`、`llmApiKey`、`llmModel`、`themeMode`（默认 light）、`animationsEnabled`（默认开启）。旧侧边栏相关常量不代表当前还有侧边栏功能。

## 4.2 草稿模型

`ParsedEvent` 包含标题、地点、开始／结束时间、全天标记、备注、来源、原文、缺失字段与置信度。开始时间可以为空，供用户补充。数据库中的事件开始时间必填。

## 4.3 写入原则

解析本身不写库。确认卡片保存与手动表单保存才创建或更新事件；失败时保留可编辑内容。丢弃未保存草稿不操作数据库；撤销已保存草稿采用软删除。

## 4.4 路径与迁移

数据库放在 `getApplicationSupportDirectory()` 返回的目录，文件名为 `calendar.sqlite`；实际路径由平台和应用标识决定，不硬编码为固定 APPDATA 子目录。

数据库使用后台连接。v1 → v2 新增 nullable 的 `deletedAt`，旧记录保持正常。后续表结构变更需要升级 schemaVersion、添加迁移步骤并验证旧数据保留。

## 4.5 设置保存

API Key 当前以明文保存在本地数据库，输入框默认遮蔽。JSON 备份不包含设置与 Key。加密存储属于按需改进项。

# 5. 本地解析引擎

## 5.1 流程

切句 → 必要时按多个时间／事项分段 → 日期时间、地点与事项抽取 → 草稿与缺失标记。无有效结果时提示补充内容。

## 5.2 日期与时间规则

| 输入 | 当前规则 |
|---|---|
| 完整年月日 | 使用指定日期 |
| 月日、未指定年份 | 使用当前系统年，已过日期不自动顺延 |
| 仅“15日／15号” | 使用当前系统年月，并检查当月有效日期 |
| 今天、明天、后天等 | 相对系统日期计算 |
| 本周／下周某日 | 周一起始，按指定周计算 |
| 无限定的周几 | 就近未来 |
| 有日期无时刻 | 全天，开始为当日 00:00，结束为空 |
| 有时刻无日期 | 默认今天；时刻已过则使用明天 |
| 单一时刻 | 解析草稿的结束时间为空 |
| 结束早于开始的区间 | 解析层按跨天处理，结束加一天 |

支持上午／下午／晚上、半点和时间区间。中文数字日期、月初／月底、节假日别名覆盖有限。精确匹配范围以 `local_parser.dart`、`regex_patterns.dart` 与对应测试为准。

## 5.3 地点抽取

优先使用“去／到／在”等语境，结合地点后缀词表，并在时间或事项片段处截断。

## 5.4 事项与标题

优先提取事项关键词和相关短语；未命中时尝试使用去除时间、地点后的主干。词表集中在 `domain/word_lists.dart`。

## 5.5 多事件

按标点／换行切句；一句包含多个由时间信息分隔的事项时进一步分段。过滤无时间的“【通知】”类标题，并排除“会议纪要”等复合名词误命中。

## 5.6 置信度

规则评分：时间 0.4、地点 0.3、事项关键词 0.3；低于 0.5 提示人工核对。缺失字段用于确认界面提示，不代表模型统计意义上的准确概率。

## 5.7 回归基准

日期边界、跨月跨年、时间段、地点截断、多事件与标题过滤用例见 `test/unit/local_parser_test.dart`。调整解析规则时同步更新相关用例。

# 6. AI 解析

## 6.1 协议

`POST {baseUrl}/chat/completions`，Bearer Key；默认 DeepSeek 配置，允许用户修改供应商、地址和模型。预设集中于 `domain/llm_providers.dart`。

## 6.2 输入与输出

system prompt 提供当前年月及日期缺省规则，要求仅返回以下结构：

```json
{
  "events": [
    {
      "title": "开会",
      "location": "会议室",
      "start": "2026-09-16T10:00:00",
      "end": null,
      "all_day": false,
      "note": null
    }
  ]
}
```

当前请求使用 `response_format: {"type":"json_object"}`，客户端解码并转换字段；没有独立的完整 JSON Schema 校验器。空标题条目跳过，无可用事件时报错。

客户端会补足缺省年月、处理全天和跨天区间。AI 草稿置信度当前固定为 0.9。具体转换行为见 `ai_parser.dart`。

## 6.3 网络参数

连接超时 10 秒、接收超时 30 秒、max_tokens 2000、temperature 0.1。网络错误、5xx、429 等可重试一次，间隔 2 秒；认证、额度等错误直接提示。常量集中于 `core/constants.dart`。

## 6.4 错误处理

提示认证失败、额度不足、限流、网络超时或返回格式异常。失败保留输入，用户可修改配置、内容或切换解析模式。

## 6.5 本地服务

AI 模式可以连接本机 OpenAI 兼容服务；“本地模式”仍专指正则解析。当前 AiParser 要求非空 Key，即使服务本身不要求认证，也需要填写占位值。

“测试连接”仅请求 `GET {baseUrl}/models`，验证服务可访问，不代表指定模型能成功解析事件。模型名称、输出格式和延迟需通过实际解析验证。

**本机部署（2026-09-16）**：i7-14650HX／32GB 内存／RTX 4060 Laptop 8GB；`E:\Ollama` 的推理核心已升级至 0.34.0（保留原桌面应用），使用官方便携包的通用与 Vulkan 组件。旧核心备份在 `E:\Ollama\backup-runtime-0.11.11`，原有 MiniMind2 模型保留。

- 模型：`qwen3.5:4b`，Q4_K_M，约 3.4GB，存放于 `E:\Ollama\models`；本机接口 `http://localhost:11434/v1`，占位 Key 可填 `ollama`。该模型本地参数已设置 `num_ctx: 8192`，覆盖旧桌面程序的 131072 默认值，避免显存不足转为 CPU/GPU 混合推理；权重不变，重新 pull 官方模型后需核对该参数。
- 命令行：`ollama run qwen3.5:4b --think=false`。日程抽取验证使用 8K 上下文、JSON 约束与关闭思考；原生 API 为 `think: false`，兼容接口为 `reasoning_effort: "none"`。已验证由原桌面程序正常启动新版核心，以及兼容接口调用后保持 8K、100% GPU。
- 在明确“改期输出新安排”规则并将 presence_penalty 设为 0 后，多事件、改期、通知截图三个样例的时间检查通过；预热后请求约 2–5 秒，首次文字与图片请求各约 40 秒。此为有限样例，不代表总体准确率或所有输入的延迟。
- 日历程序仍使用现有请求配置和 OCR→文本流程，未切换默认服务、未增加关闭思考参数或图片直传。仅在设置中填写模型名不能启用视觉解析。

# 7. OCR

## 7.1 引擎

RapidOCR，经 `flutter_onnx_ocr` 调用 ONNX Runtime，使用随包的 PP-OCRv5 检测／识别模型及字典。普通 Windows exe 即可使用，无需 WinRT OCR 语言包或 MSIX 身份。

## 7.2 处理

首次使用时懒加载，失败可重试。图片最长边超过 2000px 时等比缩小；识别后按阅读顺序排序、合并同行框及符合条件的折行，再输出文字。多图依次识别，与手输文本换行合并。

## 7.3 接入边界

`OcrService` 是 OCR 入口。暂不为其他平台增加额外实现；确有移植需求时再验证插件、字体和文件交互。

## 7.4 图片入口

文件选择、全窗口拖入和 Ctrl+V 粘贴统一进入附件列表。粘贴生成的临时文件由输入条管理，用户选取或拖入的原始文件不删除。

# 8. 输入与确认

## 8.1 状态

输入就绪 → OCR／解析忙碌 → 成功显示草稿，或失败显示错误。结果面板分别记录草稿的未保存、已保存状态。

## 8.2 界面

底部输入条最大宽 640；结果面板宽 680、最大高为视口 80%，包含卡片列表与全部保存。全部保存逐条调用卡片校验和保存，提示本次成功保存数量，失败条目保留待处理。

## 8.3 校验

标题必填；非全天事件校验时间格式与结束不早于开始。当前确认卡片使用单个日期配开始／结束时刻，不能等同于解析器支持任意跨天区间的编辑能力。缺省时间会显示表单默认值，保存前由用户核对。

## 8.4 失败与退出

解析失败保留输入；保存失败保留卡片。关闭结果面板且仍有未保存草稿时提示确认。草稿不会自动持久化为草稿箱。

# 9. 界面与设计系统

## 9.1 窗口

默认 1100×720，最小 900×600。当前启动使用默认窗口尺寸，没有实现位置／尺寸记忆。Windows 11 使用 DWM 原生圆角；自绘标题栏高 38。

标题栏拖拽／双击最大化区域与窗口按钮分开，避免单击等待双击判断。缩放使用四边／四角热区、系统绝对光标累计位移、串行合并 setBounds 更新；最小尺寸与对侧锚点由几何函数保证。

## 9.2 布局

已移除侧边栏。宽度至少 1000 时，月历与宽 320 的日期列表左右排列；更窄时上下排列。输入条底部居中，设置入口在左下角。

## 9.3 字体

HarmonyOS Sans SC 随包提供 Regular／Bold 两个字重，字体资源与许可文件见 `assets/fonts/`；缺字回退配置见 `app.dart`。

## 9.4 主题 Token

浅色／深色／毛玻璃由 `DSTokens` 与 `themeModeProvider` 控制，默认浅色。颜色、圆角和动画常量集中在 `ds_tokens.dart`。

`DSTokensScope` 必须位于 `MaterialApp.builder` 中，让 Navigator 的弹窗和 Overlay 也能访问 Token。这是已修复灰屏问题的关键约束。

### 9.4.1 浅色与深色

使用普通表面。深色以表面亮度、分隔线形成层次；通用组件通过 Token 获得主题颜色。

### 9.4.2 毛玻璃

应用内通过背景模糊、轻微放大采样、透明着色与自绘边缘近似 Liquid Glass，不是系统原生透明窗口。

| 层级 | 用途 | 当前主要参数 |
|---|---|---|
| floating | 输入条、下拉浮层、小型玻璃控件 | 基础白 14%、blur 10.5、采样放大 1.018 |
| dialog | 设置、通用弹窗、结果等 | 基础白 32%、blur 14、采样放大 1.008、默认圆角 22 |

实际表面还会叠加轻微高光。dialog 使用单一外轮廓与向内羽化反射，不绘制独立内缘线。

设置分组直接排版在同一块玻璃上，以标题、留白和控件边界组织；不要重新增加分组卡片或嵌套模糊。月历与事件列表保持清晰。发送按钮为单个着色圆盘，透出父输入条模糊结果。

## 9.5 动效与性能

普通弹窗为 150ms 淡入加 0.97→1 缩放；毛玻璃设置弹窗单独使用固定尺寸 220ms 淡入淡出，进入 easeOutCubic、退出 easeInCubic。

动画开关控制主要日期聚焦、分段切换和弹窗过渡；部分材质交互仍有自身短动画。设置配置当前在弹窗打开后异步读取，尚未采用预加载。

玻璃优化以实际窗口观感为准。修改材质、布局或窗口缩放时，应检查常用尺寸和最小尺寸下的打开、关闭与拖动过程。

## 9.6 组件

DSButton、DSTextField、DSSwitch、DSCheckbox、DSSegmentedControl、DSDialog、DSDropdown、DSDatePicker、DSYearPicker 和 DSGlassSurface 构成主要设计系统。复用已有组件；具体默认参数以组件代码和 Token 为准，避免在文档重复维护整套样式常量。

# 10. 日历与事件

## 10.1 月视图

周一起始，支持跨月补白日期、选中聚焦动画、事件圆点、翻月与年份选择。日期单击选中，今日列表“＋”负责新建；日期格不注册双击新建。

## 10.2 日期列表

随选中日期更新，全天与定时事件分组，展示标题、时间、地点与来源，提供编辑、删除入口。名称为 TodayList，但展示的不一定是系统今天。

## 10.3 表单

手动新建和编辑复用 EventForm。标题、日期、时间、全天、地点、备注由表单校验后保存。

## 10.4 数据流

EventDao 的月／日查询经 StreamProvider 提供给界面；写入后自动刷新。查询按事件开始时间归属日期，不是按与当天重叠的时间区间展开多日事件。

## 10.5 批量删除

设置 → 数据管理 → 批量删除：按月选择事件、全选或取消、确认后软删除。空列表或未选择时不能执行删除。

## 10.6 回收站

按删除时间倒序显示；单条恢复回到日历，彻底清理需确认并物理删除全部回收站事件。

# 11. 设置

左下角按钮打开宽 560、最大高为视口 82% 的弹窗，包含：

- 外观：动画总开关、浅色／深色／毛玻璃主题；修改即时保存。
- AI 服务：供应商、地址、Key、模型、连接测试；点击保存设置写入配置。
- 数据管理：ICS 导出、JSON 备份恢复、批量删除、回收站。
- 关于：版本与产品说明。

毛玻璃分组保持透明，无独立卡片轮廓。浅色／深色保留原普通卡片。已移除的主题、数据管理、字体说明小字不再作为必需界面内容。

# 12. 导出与恢复

## 12.1 ICS

仅导出正常事件，固定使用 Asia/Shanghai 时区声明；支持全天 VALUE=DATE、时间点省略 DTEND、文本转义、CRLF 与长行折行。实现见 `ics_exporter.dart`。

## 12.2 JSON

备份标识为 `app: calendar`、`version: 1`，包含导出时间和正常事件，不包含 settings、API Key 或回收站记录。

恢复按 ID 合并，实际规则如下：

| 数据库中同 ID 记录 | 行为 |
|---|---|
| 不存在 | 插入备份事件 |
| 正常事件 | 跳过，保留已有内容 |
| 回收站事件 | 恢复为正常事件，并以备份内容覆盖 |

恢复先检查文件标识、版本及事件列表，再逐条处理并统计恢复／跳过数；当前没有整批事务，不保证中途失败时撤销此前已恢复记录。

## 12.3 与 Git 的职责

Git 保存代码、文档和锁文件；用户事件通过应用内备份管理。数据库、导出文件、密钥和构建产物不提交。

# 13. 版本记录约定

## 13.1 本地与远程

当前使用本地 Git，未配置 remote。推送、创建远程 Release 等操作按用户明确要求执行。

## 13.2 提交格式

沿用 `<type>: <中文简述>`，常用 feat、fix、perf、refactor、docs、test、chore、build。每个提交聚焦一个相关意图。

## 13.3 提交时机

每项功能建立、修复、删除或其他项目变化完成相应验证后，进行本地提交。代码变动与技术文档修订通常分别提交；仅文档改动提交一次 docs 即可。提交前检查差异，只包含本轮相关文件。

## 13.4 分支

日常维护沿用现有 main；需要隔离较大改动时再创建分支，无须为每个小改动增加流程。

## 13.5 版本号与标签

文档版本、应用版本、Git 标签分别表达不同信息。当前文档 v0.61；`pubspec.yaml` 仍为 `1.0.0+1`，历史发布标签曾独立递增，不能据此推断 exe 的产品版本。

正式发布时明确目标版本并核对构建元数据。普通维护不自动新增标签。

## 13.6 忽略文件

以仓库 `.gitignore` 为准：忽略构建产物、本地数据库、备份目录和密钥等；保留 `pubspec.lock`。

## 13.7 文档修订

更新受影响的正文、文档版本、日期和修订记录，保持历史条目可追溯。历史参数不作为当前规格。无需为小改动重复描述已完成里程碑。

# 14. 常见问题定位

| 现象 | 优先检查 |
|---|---|
| 本地解析不准确 | 原文切分、日期规则、词表与对应单元用例 |
| OCR 内容断裂 | 检测框阅读顺序、同行／折行合并 |
| AI 请求失败 | 地址、模型、Key、服务返回和超时 |
| 弹窗灰屏或取不到主题 | MaterialApp.builder 中的 DSTokensScope |
| 窗口缩放跳动 | 累计位移、异步会话与 setBounds 串行更新 |
| 玻璃出现双轮廓 | dialog 外轮廓画笔、内部卡片背景或描边 |
| 设置过渡不顺 | 大面积模糊合成、异步加载时的内容重建 |
| 删除后仍显示或导出 | 查询是否过滤 deletedAt |
| 恢复结果与预期不同 | 同 ID 冲突及第 12.2 节合并规则 |

这些是维护定位线索，不代表本轮待办或必须立即重构的清单。

# 15. 验证方式

## 15.1 单元与数据测试

`test/unit/` 覆盖日期解析、AI 字段转换、OCR 合并、ICS、供应商与缩放几何；`test/data/` 覆盖 DAO、迁移、备份与设置。根据改动选择对应测试。

## 15.2 Widget 测试

`test/widget_test.dart` 与 `test/widget_batch_trash_test.dart` 覆盖主界面、输入、日期动画、设置主题、批量删除和回收站等。

## 15.3 桌面集成测试

`integration_test/` 包含日历点击、批量删除／回收站弹窗及 OCR 识别用例。涉及插件或真实桌面行为时，在 Windows 上运行相关用例。

## 15.4 验证范围

- 文档修改：核对实现、引用、修订记录与 Git 差异，无需重跑应用测试。
- 业务修改：执行相关测试和静态分析；涉及共享组件、数据库或发布时扩大回归范围。
- 界面／窗口修改：在 Release 中检查打开关闭、输入交互、1100×720 和 900×600 布局及自由缩放。
- 发布：静态分析、完整测试、Windows Release 构建，并手动走通输入→解析→确认→日历及涉及的导入导出流程。

历史 v0.59 记录为 107 项测试通过；这是当时的结果，不代表每次阅读文档都重新验证过。

# 16. 构建与发布

## 16.1 当前阶段

M0–M4 初始化与 MVP 已完成，目前按用户需求做小步维护。历史里程碑见修订记录和 Git，无须重复初始化工程。

## 16.2 常用命令

在仓库根目录执行：

```powershell
flutter pub get
flutter analyze
flutter test
flutter build windows --release
```

数据库定义变更需要重新生成代码：

```powershell
dart run build_runner build --delete-conflicting-outputs
```

桌面集成测试示例：

```powershell
flutter test integration_test/calendar_click_test.dart -d windows
```

Release 产物位于 `build/windows/x64/runner/Release/`。分发完整目录（exe、DLL、data 等），不能只复制 exe。MSIX 依赖已声明，但不代表已经配置好正式签名安装包。

## 16.3 发布记录

记录实际构建版本、产物位置与验证结果。构建 Release 与发布到远程是不同动作；远程发布需要用户明确要求。Windows CMake 已配置 UTF-8 编译，维护时保留相关设置。

# 17. 按需演进

当前优先保证输入、解析、确认、日历与备份这条核心流程。只有实际需求出现时，才考虑端侧 LLM、密钥加密、复杂日期扩展或跨平台支持。一般代码整洁度和小型结构问题随相关功能维护处理，不单独扩大工程。

---

附：开发参考以项目锁定依赖、对应工具官方文档和实际代码为准。旧版完整方案、词表草案及初始化教程可通过 Git 历史查阅；当前词表与正则直接维护在 `lib/domain/word_lists.dart` 和 `lib/core/regex_patterns.dart`。
